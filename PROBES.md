# Probes — 主動拉外部訊號

> 手動 maintain 的「需要主動去查、不能等被動社群討論浮上來」的源。Agent 跑時讀此檔，對每個 active probe 跑對應工具 fetch、結果寫進 `reports/local-analysis/{date}-probes.md`（daily channel）或 surface 進 `reports/digest-{date}.html`（stage-2-digest channel）。
>
> 主動 = 直接拉外部 source（GitHub API / RSS / web）。被動比對既有 raw md 的東西放 `WATCH.md`。
>
> **觸發**（按 entry 的 `Triggered by` 欄位分流）：
> - **`daily`**：launchd `com.gggodlin.local-analysis-probes` 每日 06:45（在 4 channel 之後）→ 結果寫進 `reports/local-analysis/{date}-probes.md`
> - **`stage-2-digest`**：每次使用者叫 stage-2 digest 啟動時 agent 主動 fetch → surface 進 digest「🆕 ... 動態」section（不依賴 06:45 daily wrapper、避免低頻訊號 spam）
> - **`both`**：兩邊都跑
> - **手動**：使用者說「跑 probes」/「probes 一下」/「拉一下 probes」→ agent 讀 PROBES.md 對所有 entries 跑（不論 `Triggered by`）

## Active

### anthropics/claude-code release watcher

- **Why**: 使用者直接用 CC CLI（cmux + 多 worktree + ad-hoc session + launchd 周邊 routine）、上游每次 release 都可能影響 TUI 行為、hook 機制、daemon、MCP timeout、quota、deprecation。要在「每次叫 stage-2 digest 的當下」即時 fetch、從上次看的版本 diff 到最新、寫進 digest「🟣 Anthropic / Claude 動態」的「🆕 CC CLI release」小節（2026-06-20 起併區、原獨立段「🆕 CC CLI 動態」收為本區第一小節）。
- **Source(s)**: <https://github.com/anthropics/claude-code/releases>（via `gh release list anthropics/claude-code`）
- **How to fetch**: `gh release list --repo anthropics/claude-code --limit 10 --json tagName,publishedAt,name`，filter `publishedAt > Last seen`；對每個新版跑 `gh release view <tag> --repo anthropics/claude-code --json tagName,publishedAt,body` 拿 changelog 前 2000c。
- **Hit signal**: 最新 release `tagName` 比 `Last seen` 新 → 有新 release。
- **Action on hit**: stage-2 digest 寫進「🟣 Anthropic / Claude 動態」的「🆕 CC CLI release」小節、列每個新 release 的 tagName + publishedAt + body 重點 entry（按 hook / daemon / MCP / quota / deprecation / fast mode / agents 等分類）+ 每條標跟使用者 workflow 對應的個人化評論 + 顆星排序（★/★★/★★★）+ 升級指令。
- **Triggered by**: `stage-2-digest` only（**不在 06:45 daily probes 跑** — release 低頻、daily 跑會 spam；digest 階段才有差分價值）
- **⚠️ 勘誤（2026-07-25，讀下方歷史 baseline 前先看）**：舊 baseline 文字多處寫「local-analysis 19-channel」，**實際為 22 個 channel**（`CHANNELS` 陣列現查為準：LLM 13 + shell 9、daily 14 / weekly-tue 8）。另舊 baseline 把 advisory 性質的 guideline 寫成「上限 / 會被限制」屬推論錯誤（例：「19 個 channel 已超過新預設的 15 上限」「貼近 20 的預設上限」）——workflow 內 `agent()` 併發本就由 runtime 排隊（cap `min(16, cpu-2)`），channel 數多於該 cap 只是排隊、不觸發攔截或警示；2026-07-25 digest 實測單次跑滿 161 agents 零攔截。歷史文字保留原樣不重寫，以本行為準。
- **Last seen**: v2.1.269（publishedAt 2026-09-11T19:17:55Z）；距前一 baseline v2.1.268（2026-09-10T20:30:54Z）共 1 個新 public release — 2026-09-12 probe baseline — 2026-09-12 probe baseline；gh release list 現查最新 public release（非 draft、非 prerelease）為 v2.1.269、publishedAt 2026-09-11T19:17:55Z，其下一筆為 v2.1.268（2026-09-10T20:30:54Z），兩者之間無其他 release；新增 `claude plugin eval`：對 plugin 的 eval suite 跑 Claude Code、得到可重現的評分結果（JSON + HTML report），用法見 `claude plugin eval --help`；新增 `/output-style [name]` 列出並切換 output style，涵蓋 Remote Control 及 cloud／其他 headless session；新增當 Bash tool 處理檔案編輯時（設定 `bashEditDiffEnabled`），Bash tool result 附上該指令改動檔案的 diff；新增 `OTEL_METRICS_INCLUDE_REPOSITORY`，為 OpenTelemetry metrics 與 events 標記 `vcs.*` repository 屬性，搭配 `OTEL_LOG_TOOL_DETAILS` 時 commit events 會加上 `vcs.ref.head.*`；新增 `CLAUDE_CODE_GATEWAY_MODEL_DISCOVERY_TIMEOUT_MS` 延長 LLM gateway `/v1/models` discovery timeout（預設 3 秒）；新增 spinner 提示建議用 `/focus` 只顯示你的 prompt、一行工作摘要與回應；新增 `CLAUDE_CODE_WORKFLOW_MAX_CONCURRENT_AGENTS`（1–256）提高 Workflow tool 每次執行的並行 agent 上限，供 inference-bound fan-out 使用；修復回應在 output-token 上限被截斷並自動 resume 後，下一輪 prompt cache 被部分作廢；修復在 Claude 思考中途被中斷後 resume session 可能改變先前 context 重送方式、傷害 prompt-cache 重用；修復 F1/F2/F4 在 kitty-protocol terminal 失效、st 的 Delete 鍵、rxvt-unicode 的 Alt+方向鍵變成 Escape、WezTerm 的 Shift+符號打出未 shift 字元（2.1.247 引入的 regression）；修復 remote／headless session 在背景 agent 仍在跑時回報「waiting for your input」（可設 `CLAUDE_CODE_BG_TASKS_REPORT_RUNNING=0` 恢復舊行為）；修復部分終端機啟動時把對 capability query（`^[[?1;2c`）的回覆顯示成雜散文字；修復 fullscreen 在調整終端機大小後 transcript 頂端或底部列變空白；修復以 `!` 開頭的 deny／ask permission rule 套用範圍超出寫入它的 settings source，現在只在自己的 source 內生效、單獨 `!` negation 會被忽略；修復 compaction 後 Claude 收到的 git status 是 session 開始時的舊狀態、非目前狀態；修復 synced plugin MCP server 在 remote session resume 時未連線；修復 resumed headless session 在 mid-turn 換 model 或 retry 請求時遺失一輪回覆；修復背景任務磁碟紀錄裡的終端機跳脫碼、換行與過大文字在 resume 時混進 task list 與 task 通知；修復 CMYK JPEG 圖片無法附加（回報「cannot decode」），現在會像其他 JPEG 一樣轉檔與縮放；修復 managed settings approval dialog 在 gRPC telemetry endpoint 未帶 scheme 時未命名 collector；修復 plugin `headersHelper` consent prompt 顯示可能被誤讀成不同 host 的 URL path；修復 plugin 錯誤把以 `@` 開頭資料夾名稱的相對 Windows 路徑顯示成 `[redacted URL]`；修復 permission-rule／auto-mode-rule／add-directory／session-rename／feedback-review 文字欄位在終端機啟用原生游標時缺游標；修復對 `/fork` receipt 在一秒內連續點擊時，session 要等目前 tool 完成才 backgrounding；修復拒絕 `shutdown` 參數的 plugin LSP server（如 rust-analyzer）在 session 結束後仍在跑，現在即使 `shutdown` 失敗也會送出 `exit`；修復 attribution reminder 覆蓋 CLAUDE.md 或 memory 對 commit／PR attribution 的禁止規則（managed settings 設定的規則仍生效）；修復日文／中文／泰文等無空格分詞語言的文字被丟失 prompt suggestion；修復 GNOME Terminal 與 Konsole 從終端機名稱推斷是否支援 synchronized output（部分版本不支援卻誤判支援）；修復 `--output-format stream-json` 結果的 `permission_denials` 漏掉被 path-scoped deny rule 擋下的 Read／Edit／Write call；修復透過 SDK 或 desktop app 跑的 session 在其他 session 的 agent list 顯示 unknown status；修復 `/insights` 在 Bedrock／Vertex／Foundry／gateway 部署（帳號無法連到預設 Opus model）失敗，改用該 session 的 model；修復另一個 Claude Code process 同時 refresh 登入時，organization policy limits 未載入；修復用 Bedrock／Vertex／gateway 的 Claude Desktop session 收不到「what Claude needs」的 turn-end 通知文字；修復設定更新只改變 server URL query parameter 順序時 MCP server 仍重新連線；修復背景 agent 名稱或描述含換行或超出終端機寬度時，prompt box 頂部邊框被拆成多行；修復 auto-compaction 找不到完整先前對話可摘要時 session 卡死在「Prompt is too long」（多見於 prompt 極大的 Agent SDK session）；修復 `/goal` 在 API 錯誤／斷網／token 上限後靜默卡住：現在會 backoff 重試，或暫停並說明原因（含等到 usage limit 重置）；修復 cloud session 的 prompt cache miss，作法是在第一個請求前先短暫等待 server configuration；修復 `/btw` 回答內含捏造的 tool call 與輸出：side question 現在被告知不要寫這些、出現的話會標記為未執行；修復 `CLAUDE_CODE_RESUME_INTERRUPTED_TURN` 重跑 6 小時前（或設定 `CLAUDE_CODE_RESUME_INTERRUPTED_TURN_MAX_AGE_MS` 更久前）因 API 錯誤失敗的 turn；修復透過 managed settings 啟用的 organization plugin 在 headless session 與 Claude Desktop（該版本 bundle 後）未載入，下一個 session 才會載入；修復 session 解壓的 plugin archive 可被其他本機使用者讀取、解壓檔案保留 archive 的 world-writable bit、重新解壓時舊檔殘留；修復 `Edit()` deny rule 與 write-path check 未套用到 Bash `tee` 指令寫入的檔案，`Bash(tee:*)` allow rule 現在不再涵蓋 working directory 外的目的地；修復連線慢時（ssh、瀏覽器終端機）啟動時把 `22c` 這類雜散字元或終端機的顏色／版本回覆打進 prompt；修復 rxvt-unicode 離開再重進 fullscreen 後 block cursor 顯示在介面下方；修復 rxvt-unicode 在 fullscreen 模式從外部編輯器返回後 cursor block 仍顯示；修復非 fullscreen 模式從外部編輯器（Ctrl+G）返回後介面被畫兩次；修復 Konsole 從外部編輯器返回後介面被畫兩次；Windows：修復送到背景的 PowerShell tool command 在 Claude Code 結束時就停止；改進 `/diff` 面板一次到位完整渲染、不再先顯示 loading 狀態；改進日文／中文／韓文的 prompt suggestion 過濾：保留混合語系與單詞建議、跟英文一樣丟掉 meta 或評價性文字；改進 Skill tool 的「Unknown skill」錯誤，在裸名稱剛好對到一個 plugin skill 時顯示該 plugin skill 的完整名稱；改進 SSH 與非常見終端機的鍵盤支援：能回應 kitty keyboard query 的終端機（如 foot、Alacritty 0.16+）現在可用 Shift+Enter 與 Ctrl+Shift 快捷鍵；改進長 session 的反應速度：transcript 更新不再為了建 collapsed tool-use summary 重新處理整段對話；改進關閉 telemetry 的第一方 session：mid-conversation 連線完成的 `alwaysLoad` MCP server 在下一輪就可直接用、不必再走 tool-search round trip；`/ultrareview --post` 改為 findings 到齊時直接貼 PR comment 並印出 comment link，不再另開第二個 cloud session 去貼；改為存進 session scratchpad 的 artifact database 讀取不再卡 working-folder approval；改為 cloud session 從 claude.ai 同步的 skill 命名為 `anthropic-skills:<name>`（比照 Claude Desktop），沒有其他東西用同名時裸名稱仍可用；changelog 另含 [VSCode] 23 條、[Claude Code on the web] 7 條、[Claude Tag] 10 條分類條目，涵蓋 agent map、Hooks dialog、Permission rules dialog、Focus view live progress、session list／usage meter／prompt cache clock 顯示同步，以及 Claude Tag 對 Slack rate-limit 失敗通知重試、model 限制拒答、table 附檔顯示、`@Claude !restart`、plugin 存取設定顯示、共享 session banner 對象標示、admin settings 載入速度、routine 回覆同串 thread、進度時間戳本地化
- **Baseline history**（新→舊、由 writeback helper 自動維護）:
  - v2.1.268（publishedAt 2026-09-10T20:30:54Z）；距前一 baseline v2.1.267（2026-09-09T19:58:14Z）共 1 個新 public release — 2026-09-11 probe baseline；gh release list 現查最新 public release 為 v2.1.268、publishedAt 2026-09-10T20:30:54Z，其下一筆為 v2.1.267（2026-09-09T19:58:14Z），兩者之間無其他 release；新增 Claude apps gateway：在 gateway.yaml 設 pricing: 後，已登入的 Claude Code client 透過 managed settings 收到相同費率，使 /cost 與 telemetry 與 spend meter 一致（原提案漏「透過 managed settings」這個限定）；新增 gateway 在 access_control.allow_cidrs 為空時的啟動警告，以及首次收到來自公網位址請求時的一次性警告；新增 gatewayInternalNetworks managed setting，讓管理員允許 /login 到架在組織自有公網 IPv4 網段上的 Claude apps gateway；新增 claude self-hosted-runner --remove-session-state（預設關閉）：session 結束時刪除 <base-dir>/_sessions/ 下該 session 的目錄；claude auth status --json 輸出新增 configDirectory；claude plugin install / uninstall / update / enable / disable 新增 --json，claude plugin list --json 每列新增 errorDetails 與 noteDetails；已發佈 artifact 新增瀏覽器分頁圖示，由 Claude 依各頁面內容挑選；修復自 2.1.265 起第三方 Anthropic-compatible endpoint（ANTHROPIC_BASE_URL）每個 turn 都失敗於 HTTP 400，起因是 Artifact tool input schema 內一段被這類 endpoint 拒絕的 regex；修復 WebFetch 對「保持回應開啟卻不結束」的伺服器無限期掛住，現在 300 秒後失敗，可用 CLAUDE_CODE_WEBFETCH_DEADLINE_MS 覆寫、設 0 關閉；修復 respawn 的 in-process teammate 會從未信任資料夾內同名 agent 檔取得 tools 或 system prompt；修復持續高 CPU：長時間閒置 session 的 busy loop 不再佔滿一個 CPU 核心，session recap 期間快速的 terminal focus report 也不再讓 CPU 維持高檔；修復 MCP tool call 後 Claude 有時回覆「your message came through empty」；修復 symlink 目錄（macOS 的 /etc、/tmp、/var，Linux 的 /bin）的 deny 與 ask permission rule 在以真實路徑指定時不生效，以及 Bash 指令忽略以 symlink 路徑寫法設定的 deny rule；修復同一行出現 env -C、eval 等 permission checker 無法分析的指令時，Read 或 Edit 的 deny rule 不生效；修復 plugin 與 marketplace 錯誤訊息顯示 git source URL 裡的 token 或密碼；修復 /mcp 與 /plugin server 詳情、claude mcp list/get 以及 MCP login 錯誤顯示由 MCP config 中 ${VAR} 解析出的密鑰；修復使用 excludeDynamicSections 的 SDK session 在 mid-session 中斷 prompt caching 與 extended thinking，第一則訊息不再每次請求重新 render；修復有權限的使用者在重啟後或 Desktop Code tab 中，因快取的 model-access denial 過期未更新而被誤告知 model 受限；修復另一個 Claude Code process 刷新過期 model-access entry 時，執行中的 session 會靜默切換到組織預設 model；修復 Fable model 的長 context 429 在 Pro 與 Team plan 上顯示 usage-credits consent prompt，而非 1M-context 訊息；修復透過 profile 設定 workload identity federation（如 claude-code-action 的設法）時，共用同一 profile 的多個 process 可能在執行中因 401 … jti reused 失敗；修復本機 callback port range 無法綁定時，MCP server OAuth 登入失敗並顯示 No available ports for OAuth redirect；修復 /compact 與 auto-compact 產生的對話摘要在文字含 $ 序列時損毀；修復以 /compact 結束的對話 resume 時，還原檔案的 notes 每次 resume 載入順序不一致；修復 SDK prompt suggestions、side questions 與 /rename 送出 compaction 之前的舊對話；修復用上鍵叫回前一則 prompt 並編輯後，@ 檔案與 / 指令建議不再出現；修復 claude agents 以正常節奏連按 ← 回到 agent list 時，按鍵會被忽略直到停頓超過一秒（原提案僅寫「快速連按 ← 回到 agent list」、未寫出修復內容）
  - v2.1.267（publishedAt 2026-09-09T19:58:14Z）；距前一 baseline v2.1.266（2026-09-08T23:55:14Z）共 1 個新 public release — 2026-09-10 probe baseline；新增 `maxEffortLevel` 設定，可放 top-level 或 per-model `modelSettings` 下，在包含 Bedrock、Vertex、Foundry 的所有 provider 上限制 effort level，使用者仍可選較低等級；新增 `--system-prompt-snapshot off`，讓每次請求重新產生 system prompt 而非複用對話已記錄的版本，供迭代 prompt 文字用；修復 managed settings 要求 sandboxing 的組織其 Cowork cloud scheduled tasks 啟動失敗；修復 `/context` 等本地指令輸出在行動端 client 顯示空白；修復重連 tmux 或 ssh session 後 agent view 內 shift+enter 與 option+backspace 失效；修復 fullscreen 模式向上捲動時對話頂端未顯示前次 prompt 的淡色標頭；修復 Workflow `agent()` 大型 output schema 在 auto mode 被直接拒絕、而非交由安全分類器判斷；修復 marketplace entry 路徑含反斜線可在 macOS 與 Linux 繞過 fetched marketplace 的 containment check；修復 Claude Desktop 等 host app 下 AWS 或 Google Cloud 憑證過期時，先重試十次並顯示通用 request failed 才出現重新認證錯誤；修復以 `-p --resume` 在 `/compact` 或其他 slash command 後恢復 session 時插入多餘的「Continue from where you left off.」turn；修復恢復大型 session（transcript 超過 5 MB）時 parallel tool call 與其 hook output 從重載對話中被丟失；修復 **managed** 的 `allowedHttpHookUrls`、`httpHookAllowedEnvVars`、`allowedChannelPlugins` 在設定不可讀時改為預設全部拒絕而非全部放行（原 proposed 漏「managed」限定詞，來源限定為 managed settings）；修復 managed settings 要求 Claude apps gateway sign-in 的機器上 `/login` 對話框按 Esc 無反應；修復 artifact publish 因上傳中連線中斷而中止時，改為在 Claude Code 能判定上傳未完成時重試一次，而非回報未知結果；修復自訂 command、skill、subagent 的 `effort:` frontmatter 在 default effort 仍被釘死的 model（Opus 4.7、Opus 4.8、Fable 5）上被忽略；修復 `claude agents` 的 `@` 目錄選單未列出 session 啟動後才新建的 repo；修復加入 Claude Desktop 或 VS Code session 的 Remote Control client 顯示過期的 permission mode；修復 `claude remote-control` 在 server credential 過期（約啟動後 30 天）時退出並中斷所有 attached session，改為由 host 重新註冊後繼續；修復同一 session 內不同 model 或 mode 回報不同 limit window 時 usage-limit 警告閃爍；另有一組 prompt-cache 相關修復：mid-session 新增的 MCP／plugin tool 在無 ToolSearch 的 session 中改以 deferred definition 提供、`/model` 切換不再重送全部 tool definition、`-p` 對話改互動式恢復時 system prompt prefix 不再改變；Gateway 變更：`forward_user_identity` upstream 對已轉發 email 的開發者直接回傳 429，不再 failover 到下一個 upstream，使 proxy 的 per-user limit 生效
  - v2.1.266 (2026-09-08T23:55:14Z)；v2.1.265 (2026-09-08T20:37:31Z) — 2026-09-09 probe baseline，距前一 baseline v2.1.263 共 2 個新 public release — 2026-09-09 probe baseline；距前一 baseline v2.1.263，anthropics/claude-code 共新增 2 個 public release：v2.1.266（publishedAt 2026-09-08T23:55:14Z）與 v2.1.265（publishedAt 2026-09-08T20:37:31Z）。；v2.1.266 的 release notes 只有一條 bullet，內容是修復 2.1.265 影響 LLM-gateway 與 proxy 設定的 regression。；未文件化的環境變數 CLAUDE_CODE_USE_GATEWAY 原本除非 ANTHROPIC_BASE_URL 與 ANTHROPIC_AUTH_TOKEN 兩者都有設定、否則會被忽略（原 proposed 寫「兩者都未設時才生效」為反向錯誤）。；2.1.265 起 CLAUDE_CODE_USE_GATEWAY 單獨存在就會強制 Cloud-gateway 登入，導致同時設 API key、apiKeyHelper 或自訂 auth header 的設定每個請求都失敗並回報「Not signed in to the Cloud gateway」。；v2.1.266 已讓 CLAUDE_CODE_USE_GATEWAY 單獨存在時恢復被忽略，使用者不需更動任何設定。；v2.1.265 新增 --plugin-dir 指向 plugin 資料夾集合的支援：內含 manifest 的每個子資料夾各自載入，執行中新增或移除的子資料夾也會被偵測到。；v2.1.265 新增工具結果落地存檔的 1 GB 上限，對話內預覽會標示存檔已被截斷。；v2.1.265 修復 resume 由前景 spawn 的 subagent 時工具清單與 system prompt prefix 改變、破壞該 agent 的 prompt-cache 重用。；v2.1.265 修復 agent teammates 與 resumed subagents 在後續回合把 SubagentStart hook context 與預載 skills 移出 prompt prefix、破壞 prompt-cache 重用。；v2.1.265 修復前一個 process 在工具執行中死掉後的 resume：最後一則 prompt 不再被改寫，被中斷的 tool call 會保留並標記為 interrupted。；v2.1.265 修復 /model opusplan[1m] 被回絕為「Model not found」。；v2.1.265 修復 plugin 路徑含反斜線可在 macOS 與 Linux 繞過 symlink containment check。；v2.1.265 修復名稱以兩個點開頭的 plugin 目錄被誤判為在 plugin root 之外。；v2.1.265 修復 forked skills（context: fork）沒有把 kickoff prompt 以 stream-json progress event 串流出來，以及 --forward-subagent-text 時其文字回合同樣沒有串流。；v2.1.265 修復 advisor 工具與其指示依每次請求所用 model 重新判定的問題，改為只判定一次、變動時在對話中宣告。；v2.1.265 修復 artifact publish 接受 connector 實際未提供的工具名稱：宣告的工具全不存在時拒絕發佈、只有部分不存在時發出警告。
  - v2.1.263 (2026-09-06T02:54:20Z)；v2.1.261 (2026-09-04T19:58:10Z)；v2.1.260 (2026-09-03T23:48:12Z)；v2.1.259 (2026-09-02T22:33:51Z)；v2.1.258 (2026-09-01T22:33:20Z)；v2.1.257 (2026-09-01T17:53:52Z)；v2.1.252 (2026-08-31T19:46:55Z) — 2026-09-07 probe baseline，距前一 baseline v2.1.251 共 7 個新 public release、跨 2026-08-31 至 2026-09-06 — 2026-09-07 probe baseline；v2.1.263 的 release notes 只有一條「Bug fixes and reliability improvements」、無詳細 bullet。；v2.1.257 新增 Claude Fable 5.1（`claude-fable-5-1`）並成為預設 Fable model，1M context、$10/$50 per Mtok、cache reads $0.25/Mtok。；v2.1.257 新增 `CLAUDE_CODE_SUBAGENT_MODEL_FORCE`，把 `CLAUDE_CODE_SUBAGENT_MODEL`（或主 model）套用到每一個 subagent，忽略 per-spawn 與 agent 定義的 model override。；v2.1.257 為 auto mode 新增 Containment Escape 規則：cloud metadata-credential 抓取、egress evasion 與 cross-tenant 觸及不再自動核准，除非環境已標示這些行為是預期的。；v2.1.257 在 `/effort` 新增 `s`，只改當前 session 的 effort，與 `/model` 一致。；v2.1.258 修復 Claude Code 在 macOS 12 (Monterey) 無法啟動，該問題是 2.1.255 引入的 regression。；v2.1.259 新增 `managedMcpServers` managed setting，組織可對所有使用者提供 HTTP/SSE MCP server，entry 形狀同 `.mcp.json`、指定要執行命令的 entry 會被略過。；v2.1.259 新增 `--permission-prompts none` 供無人值守 headless host 使用：任何本會跳出提示的動作自動被 deny，現行 permission mode（含 auto mode）仍繼續做判定。；v2.1.259 修復並行 session 互相靜默還原彼此對 `~/.claude.json` 的改動，workspace trust 不再重置、MCP 與 project state 不再遺失（來源只講「同時跑很多 session」，未限定共用同一 project 目錄）。；v2.1.259 修復 claude.ai 使用者每次啟動都重跑的背景 GitHub connection check，結果改為跨啟動記住。；v2.1.260 新增在 fullscreen 模式與對話並排的 diff 面板，用 `/diff` 切換、Claude 編輯時即時顯示未 commit 的改動。；v2.1.260 在 `/cost` 與 status line 的 `prompt_cache` 欄位加上 prompt-cache miss 的可能原因（如 tool definitions 或 system prompt 變動、閒置超過 TTL）。；v2.1.260 為 headless session 新增 `/reload-plugins`，並新增文字版 `/advisor`（`/advisor`、`/advisor <model>`、`/advisor off`）。；v2.1.260 修復 Claude Fable 5.1 的 prompt caching 未涵蓋 tool result 之後附加的 context，導致每個 tool-call turn 都以未快取的 input 重送。；v2.1.260 修復 `/model` picker 對有權限使用的組織未顯示 Fable 5.1，先前只能打 `/model claude-fable-5-1` 才被接受。；v2.1.260 回退 v2.1.259 把 `Read()` deny rule 套用到 Bash 參數的改動：該改動在所有 mode 下用 `Read(./**/build/**)` 擋掉 `npm run build`，並讓 `cd … && grep` 在 auto mode 也跳提示。；v2.1.261 新增 `bashOutputMaxChars` 與 `taskOutputMaxChars` 設定，可把 Claude 直接收到的 inline 指令與背景任務輸出上限提高到 128K 字元。；v2.1.261 新增 `/skill-doctor`，顯示哪些已載入的 skill 未被使用、以及它們花掉多少 context，可據此瘦身。；v2.1.261 新增 `--append-subagent-system-prompt-file`，從檔案讀取 subagent system prompt，供太大而無法走命令列的 prompt 使用。；v2.1.261 修復 resume session 遺失 parallel tool call 周邊的 hook output 與其他 context，該遺失會改變 resumed request。；v2.1.261 修復 `SendMessage` 送到另一台機器上離線的 Remote Control session 時被讀成已送達，結果現在會說明送達排隊中、直到該機器重新連線。；v2.1.252 修復失敗輸出極大的背景任務通知（例如滿硬碟時的 git error）使對話超過 API request size limit。；v2.1.252 修復「always allow」在尚無 `.claude/settings.local.json` 的專案不儲存。
  - v2.1.250 (2026-08-28T00:49:16Z)；v2.1.251 (2026-08-28T18:19:32Z) — 2026-08-29 probe baseline；v2.1.250 的 release notes 僅列出「Bug fixes and reliability improvements」。；v2.1.251 新增 PreModelSwitch 與 PostModelSwitch hook events，可 block、confirm 或 annotate model switch。；v2.1.251 的 SessionStart resume hooks 會收到 session staleness 與 estimated re-cache cost。；v2.1.251 對 Remote Control clients 新增 foreground subagent 的 tool calls/results live streaming。；v2.1.251 的 background subagents（預設）仍只顯示 status。；v2.1.251 在 /usage 新增 Spend limit bar，並提供 rate_limits.spend_limit status-line field 給 Claude apps gateway spend limits 使用者。；v2.1.251 在 /cost 新增 per-session prompt-cache line，包含 hit ratio、misses、tokens re-cached 與 warm/cold 狀態。；v2.1.251 將 attach、logs、stop、respawn 與 rm 加入 claude --help。；v2.1.251 修復 Read、Write、Edit 在 permission check 後工作目錄內 symlink 被替換時可讀寫 approved location 外部的問題。；v2.1.251 修復 marketplace entry 宣告的 plugin commands 指向 plugin directory 外部路徑的 path traversal。；v2.1.251 修復 Workflow 在 permission check 前讀取 session 不可讀取的 scriptPath 外部路徑。；v2.1.251 修復 SDK 與 cloud sessions 在 SDK MCP server handshake acknowledgment 遺失時無限等待，改為 70 秒 timeout 並只標記該 server failed。；v2.1.251 修復 session transcript 因目錄變更移至既有同 ID transcript 時被靜默覆寫。；v2.1.251 修復 TUI 有許多 parallel subagents 時的 lag，讓每秒 progress tick 取代前一個 tick。；v2.1.251 修復 background sessions 與其 subagents 無法編輯由 git worktree add 建立的 git worktree 內檔案。；v2.1.251 將 CLAUDE_CODE_SUBAGENT_MODEL 改為設定預設 subagent model，agent definition 的 model 與 explicit per-spawn model 優先。；v2.1.251 讓 /effort 按 model 儲存 default effort level。；v2.1.251 禁止 project-level .claude/settings.json env 設定 CLAUDE_CONFIG_DIR、CLAUDE_CODE_TMPDIR、TMPDIR、TMP 或 TEMP。；v2.1.251 要求 server-managed 中會終止 sandbox TLS、路由 sandbox traffic through proxy、注入 credentials 或弱化 sandbox isolation 的設定，在套用前取得核准。
  - v2.1.248 (2026-08-27T22:12:20Z)；v2.1.247 (2026-08-26T23:06:39Z) — 2026-08-28 probe baseline；`gh release list` 回傳 v2.1.248 的 publishedAt 為 2026-08-27T22:12:20Z。；`gh release list` 回傳 v2.1.247 的 publishedAt 為 2026-08-26T23:06:39Z。；v2.1.247 與 v2.1.248 都晚於 PROBES.md entry 的 Last seen v2.1.246。；v2.1.248 的 `--restricted`／`CLAUDE_CODE_RESTRICTED=1` 會移除執行命令或程式碼的內建 tools。；v2.1.248 的 `--restricted` 會移除 `WebFetch`，除非它列在 `--tools` 中。；v2.1.248 的 `--restricted` 會忽略 user、project 與 local settings files。；v2.1.248 在 agent frontmatter 新增 `experimental.cacheTtl`，可用值為 `5m` 或 `1h`。；v2.1.248 在 Bedrock、Vertex、Foundry，以及 telemetry 停用時，支援同一機器 session 間的 `SendMessage`／`ListAgents` cross-session messaging。；v2.1.248 修復 long session 中約每小時發生的 prompt-cache miss。；v2.1.248 將該 prompt-cache miss 歸因於 OAuth token refresh 後重新 render tool definitions。；v2.1.248 修復 account 進入 usage overage 時 `ScheduleWakeup` tool definition 在 session 與 `--resume` 間變動造成的 full prompt-cache miss。；v2.1.248 讓 `/ultrareview` 與 locally seeded cloud sessions 不再把未提交的 `prod.env`-style／`*.tfvars` edits 上傳。；v2.1.248 讓 `/ultrareview` 與 locally seeded cloud sessions 不再把 credential files 的 editor swap、temp、backup copies 上傳。；v2.1.248 讓 background worktree session 在執行期間持有 worktree lock。；v2.1.248 對無效 `crossSessionInbound` 在 user settings 下警告並 hold cross-session messages，在 managed settings 下拒收。；v2.1.248 將 Workflow tool prompt footprint 從約 5.7k tokens 降到約 1k tokens。；v2.1.247 新增 `SendFeedback` tool，能在 `/feedback` 讓 Claude 擬定供使用者審閱與送出的 feedback report。；v2.1.247 新增 `/claude-api cost-optimize`，用來分析既有專案的 Claude API spend 並逐一處理 caching、token hygiene、batch、effort、model choice 成本槓桿。；v2.1.247 更新 `/claude-api` skill，涵蓋 organization members、invites、workspaces、API keys、rate limit reports、workload identity federation 與 CMEK 的 Admin API。；v2.1.247 讓 sub-agents 遇到首次呼叫 model 404 時改用 session fallback model chain。；v2.1.247 修復 hook 或 background agent 印出 megabytes error output 時可能 overflow conversation 並卡在 `Prompt is too long`。；v2.1.247 將 Sonnet 5 的 default auto-compact window 改成完整 1M context。；v2.1.247 讓 cross-session peer messages 預設摺疊成一行 preview，Ctrl+O 可展開全文。；v2.1.247 讓 marketplace-supplied text 在 `/plugin` 與 `claude plugin` output 中 escape-safe。
  - v2.1.246 (2026-08-25T22:31:52Z)；v2.1.245 (2026-08-25T05:13:24Z) — 2026-08-26 probe baseline；v2.1.246 新增 Bash wildcard allow rule 啟動警告（提醒 subcommand 前 wildcard 也會匹配插入的 options）、/permissions Auto mode 分頁與回合完成時間，並修復 fullscreen/transcript 效能與捲動、background session 啟動、headless/remote sessions 的 MCP 中斷錯誤回報、空 schema 參數型別及 dynamic workflow 重啟提示；v2.1.245 修復 glibc 2.44 Linux 發行版啟動 crash。升級指令：claude update（本次未實測本機版本）
  - v2.1.243 (2026-08-24T23:40:26Z) — 2026-08-25 probe baseline（距前版 v2.1.241 約 47 小時、共 1 個新 public release、60 條 bullet；`gh release list` 現查顯示 v2.1.242 無對應 public release、直接跳號；重點：修復 hook `if` 條件（如 `Bash(cat *)`）在指令含 `$()`／反引號 command substitution 後接更多參數時誤判命中；修復背景 subagent 在最後一個背景 Bash 任務完成後沒有醒來繼續；修復 session 對 Anthropic API 完全不回應時卡 10+ 分鐘無聲（改為約 3 分鐘逾時＋重試一次）；新增 `promptCacheTtl`／`subagentPromptCacheTtl`（主對話 1hr、subagent 5min）與 `modelPricing` managed setting；`/model` picker 與 claude-api skill 把 Sonnet 5 $2/$10 per Mtok 列為標準價非促銷；sandboxed Bash tool 改為不再列出允許網路 host 清單（直接嘗試＋即時核准）；修復 sandbox network-violation 細節在被擋指令仍 exit 0 時從 Bash tool result 消失；修復 `--agents` 靜默忽略無效 JSON；新增 `/usage` Loops 明細。⚠️ 本條由 main session 於 2026-08-25 手動回寫——workflow 的 `daily-topic-probes-writeback.mjs` 對含控制字元的 payload 在 `JSON.parse` 拋 SyntaxError（position 4070）而崩在寫檔前，helper bug 未修）
  - v2.1.241 (2026-08-23T00:52:16Z) — 2026-08-23 probe baseline（距前版 v2.1.238 約 52 小時、共 3 個新 release；v2.1.239 有 59 條 bullet、v2.1.240/v2.1.241 各 1 條通用 bug fixes/reliability；重點：新增 `/claude-api upgrade` 與 Python SDK 1.x 參考、修正 WebFetch 過期內容佔滿 session 記憶體、刪除 working directory 後 hooks 仍可執行、MCP elicitation 表單與 remote MCP 重連、跨 session `ListAgents`/`SendMessage` 可見性、`/goal` 長跑 check-in backoff 與 goal resume、readline Bash word keys，以及 OpenTelemetry PreToolUse trace 延續；另 cost estimate 納入 US-only inference 1.1× premium。升級指令：CC 具備自動更新，如需強制檢查可跑 `claude update`（本次未實測本機版本，只查 GitHub release API））
  - v2.1.238 (2026-08-20T20:33:51Z) — 2026-08-21 probe baseline（距前版 v2.1.237 約 20 小時、共 1 個新 release、39 條 bullet；重點：新增 `keybindingFlavor=readline`，讓 Ctrl+W 回到上一段空白（★★，命中 Bash/CLI 操作）；plugin marketplace/catalog 的 `headersHelper` 可產生短效 HTTP header，install/update 會顯示 command 並要求確認（★★，命中 plugin/supply-chain trust）；self-hosted runner 新增 defer-shutdown 與 proxy authorization command/file（★，未使用 runner）；修復長互動 session 的 subagent tool result 記憶體無界增長、custom/project/plugin output style 漂回預設、近 usage limit 時 prompt suggestion 關閉、worktree Bash 誤報 redirect、runner slow poll 被移除、MCP elicitation 長 URL 與 permission prompt dont-ask-again 選項、暫存 cwd 殘留、stdio MCP 在 initialize 前 discover、長 session startup 等（★★★，命中長 session、MCP、workflow 穩定性）；cross-session messaging 對拒收或 inbox 丟棄現在回報 sender，ListAgents/SendMessage 也修正 Remote Control/server peer 與 idle worker 顯示（★★★，命中本 repo 跨 session 協作）；更新 bundled `claude-api` skill、project MCP headersHelper 信任與 credential env 邊界、disabled MCP list/get 不再連線 health check（★★，命中 skill/MCP 安全）；升級指令：CC 具備自動更新，如需強制檢查可跑 `claude update`（本次未實測本機版本，只查 GitHub release API））
  - v2.1.237 (2026-08-20T00:54:41Z) — 2026-08-20 probe baseline（距前版 v2.1.235 約 29 小時、共 2 個新 release、v2.1.236 有 33 條 bullet、v2.1.237 有 2 條 bullet；重點：v2.1.237 新增內建「Concise」output style，可在 /config 選用，讓 Claude 省略前言與過程敘述但維持工作完整度（★★★，命中使用者偏好的刪墊字與白話輸出規則，需實測是否影響 digest/report 內容）；修復 LLM gateway 或 custom base URL session 的 prompt caching（★★★，命中 cc-vendor-bridge 與第三方 endpoint 長 session 成本/可靠性）；v2.1.236 新增 ANTHROPIC_DEFAULT_MODEL，控制新 session 預設 model 且 /model 覆寫並跨 restart 保留（★★★，命中 model routing）；新增 cross-session SendMessage 的 `notify_when_idle` 一次性通知（macOS/Linux、opt-in、不需 polling）（★★★，命中本 repo SendMessage/背景 agent 協作）；macOS sandbox wildcard read-deny 規則在 allowed read region 內優先、涵蓋 matched directory contents 且無法藉改名繞過（★★★，命中本機 secret/sandbox 防護）；修復 session working directory 被刪後 clipboard、background housekeeping/sessions、local MCP logs 與 SDK/VS Code skills hot-reload 故障（★★，命中 worktree/session lifecycle）；另修復 fullscreen renderer、/model picker、SendMessage malformed closing tag、subprocess promise rejection、managed-settings approval、tmux title、auto mode、/goal、usage、Remote Control 等問題，並改善 startup、auto mode 與 SendMessage inbox backpressure；升級指令：CC 具備自動更新，如需強制檢查可跑 `claude update`（2026-08-20 未實測本機 `claude --version`，只查 GitHub release API）
  - `v2.1.235 (2026-08-18T20:38:54Z)` — 2026-08-19 probe baseline（距前版 v2.1.234 約 24 小時、僅 1 個新 release、共 19 條 bullet；重點：新增可選 `spellcheck` 設定，使用已安裝的 `aspell`、`hunspell` 或 `ispell` 在 prompt 輸入時標示拼字錯誤；修復 language server 斷線或重連造成的 whole-prompt cache invalidation、深層 markdown list 對齊與 wrapped list hanging indent、多行 prompt highlight 位移、permission prompt comment field 的 Shift+Tab 誤授權、Agent tool 在 general-purpose agent 不可用時錯誤宣告 default、notebook cell approval 缺既有內容、回應期間 slash command 顯示 HTML entities、background auto-update 後 footer 不顯示 restart notice；修復 resume/relaunch 時仍有 open tasks 的 expanded task list 預設摺疊；改善 `/ultrareview` 與 `/autofix-pr` 等 cloud session 背景 event stream 的 CPU/memory、permission dialog 的 grant scope 與 don't ask again 顯示一致性、native macOS/Linux grep 的 pathological pattern 與 `-m N` context 輸出、context-limit error 對 auto-compact 設定的提示、Vim mode transcript/panel 狀態、快速連按 arrow/Enter 的選項選取；SendMessage 會先拒絕過大的 cross-session message；Remote Control 的 `claude rc` 套用 enterprise-gateway availability check；VSCode 修復多 Claude tab restore/reload 時 focus 自行跳動。升級指令：CC 具備自動更新，如需強制檢查可跑 `claude update`（2026-08-19 未實測本機 `claude --version`，只查 GitHub release API）
  - `v2.1.234 (2026-08-17T20:20:58Z)` — 2026-08-18 probe baseline（距前版 v2.1.233 約 3 天／70 小時、僅 1 個新 release、共 51 條 bullet；重點：Claude Code 在 claude.ai usage limit 重置時自動接續 session，`/config` 新增「Continue automatically at usage limit」開關可關閉（★★★★，直接命中 session-monitor / workflow-monitor skill 的核心痛點——背景 workflow 撞 5h 額度需人工續跑，這個官方 native 化的自動續跑機制可能讓 session-monitor watcher 部分功能冗餘，值得下次碰到長跑 workflow 時重新評估 session-monitor skill 的必要性，但注意本機制是 claude.ai usage-limit reset 觸發、跟 workflow-monitor 針對背景 Workflow 的 pause/resume + journal cache 機制不完全同一件事，兩者是否重疊需實測）；修復第三方 gateway 的 non-streaming fallback 路徑上，API response 缺 thinking/text field 時會 crash（★★★★，直接命中 cc-vendor-bridge——ccp-glm/ccp-gpt/ccp-bruce 這類第三方 endpoint 派工走的正是這條路徑）；`claude-api` skill 內建 context 成本從 ~200k+ tokens 降到 ~25k（改按需載入 reference docs）（★★★，⚠️ 2026-08-18 修正原 probe 的行為宣稱：本 repo **查無觸發紀錄**——實查 213 個 session jsonl 內 `claude-api` Skill 呼叫 **0 次**（同一枚舉下 workflow-monitor 146、research-before-answer 17），`daily-topic-analysis.js` 亦零引用。該 skill 檔頭雖標「TRIGGER — read BEFORE opening the target file」，但本機主力 Opus 實測會自發跳過它（見 wiki cc-vendor-bridge-toolkit）。所以這次降的是**潛在**成本、不是已在燒的成本；引用本條時不要再寫成「大量觸發」）；移除 `/config` 的「Default teammate model」設定，agent-team teammates 現在預設繼承 leader 的 model（除非 spawn 時明確指定）（★★★★，命中 Subagent 模型選擇規則體系——routed-impl/routed-judge/fork 等 model pinning 邏輯建立在「派工時可指定/繼承 model」的假設上，需留意這條規則講的是 agent-team teammate 模式、與本 repo 慣用的 Agent tool 派 subagent 是否同一機制尚待查證）。次要（★★★）：修復 background subagent tool permission prompt 回答時 session-scoped 權限答案（含 deny）被丟失（命中大量 fan-out subagent 場景）；修復 `SendMessage` 在 session 名稱撞 200 字上限或 emoji 密集時拒收從 `ListAgents` 複製來的 recipient、`SendMessage`／`ListAgents` 現在會明講「帳號 session 清單太長沒查完」而非把未見 session 當不存在、修復 Claude Desktop 跨 session 訊息在 cross-session messaging 被讀為 disabled 時被接收方 session 靜默丟棄導致發送方卡在「thinking」數分鐘（三項皆命中本次任務所在 session 實際使用的 SendMessage/ListAgents 機制可靠性）；修復 MCP diagnostics 印出 resolved secrets（scope-conflict 警告改顯示 `${VAR}` 原形、連線失敗只顯示 server origin）（命中本 repo 掛多個 MCP server 的憑證安全紀律）。一般相關（★★）：修復 auto mode 長 session 在對話被 compact 後反覆重新檢查並拒絕 sandboxed 指令的網路存取；改進「API returned empty/malformed response」錯誤訊息附上 content type/body kind/size/request ID 與原始 streaming 失敗原因（命中 gateway 可靠性 debug）；`/goal` 在 turn 因不可恢復錯誤（額度用盡／context overflow 等）死掉時自動清空並提示不再卡在武裝狀態、`/goal` 背景任務等待 30+ 分鐘會主動 check-in（可設 `CLAUDE_CODE_GOAL_CHECKIN_MINUTES=0` 關閉）（兩項命中 goal-gen skill 場景）；修復「Try new fullscreen renderer」提示被接受後重啟 session 遺失 permission mode/工具 allow-deny/model/effort flag；修復 `/tui` 重啟時遺失啟動 `--allowed-tools`/`--disallowed-tools` 規則；修復 trust prompt 在目錄先於 repo 存在時漏顯示 repo-wide scope 警告（命中「不可逆/破壞性動作」授權邊界紀律）；修復 IDE diff tab 在權限 re-prompt 期間關閉導致用前次輸入回答新提示；provider API token 在 shell delimiter 後也能正確 mask；Background task 通知在 turn 之間改用 `<\system-reminder>` tag 包裹傳遞、與 turn 中傳遞方式一致；修復 `strictKnownMarketplaces` allowlist 誤放行 host 與 git 實際連線 host 不同的 SCP-style marketplace source。其餘低相關（★，未逐條展開）：`CLAUDE_CODE_PROJECT_DIR_NAME` env var（多 host per-project transcript dir 命名，未見多 host 場景）／`selection:clear` keybinding／GitLab MR badge（本 repo 皆 GitHub origin）／Claude 帳號 email 使用政策聲明（與本 session system prompt 既有政策一致、非新行為）／Windows NT-namespace 路徑安全修復（Windows 專屬）／markdown 渲染 unusual Unicode 變慢修復／repo host 偵測 unusual userinfo 修復／modal OAuth URL 複製漏字／`---` 分隔線渲染修復／連續 shell command 拆多行修復／`!` shell 執行中對話框被誤關／queued `!` shell command 編輯後變 plain text／queued messages 重複顯示等一組修復／Remote Control 系列共 5 項修復（未見使用者用 Remote Control）／expired credential 導向 `/login`／transcript 自己 prompt 也 render markdown／auto-generated session title 改精簡命名／`/permissions`／`/add-dir` 等 dialog 可在 working 中開啟／`claude setup-token` 拒收多餘參數／Esc 不再清滑鼠選取／移除 auto mode classifier 提示行／elapsed-time counter 變暗／Mantle admin-pin probe 略過／Windows `.claude.json` 唯讀重啟不卡死（Windows 專屬）。以上逐項對帳 `gh release view v2.1.234` 的 51 條 bullet、無漏項。升級指令：CC 具備自動更新，如需強制檢查可跑 `claude update`（2026-08-18 未實測本機 `claude --version`，只查 GitHub release API）
  - `v2.1.233 (2026-08-14T22:20:57Z)` — 2026-08-15 probe baseline（距前版 v2.1.232 約 23 小時、僅 1 個新 release、共 20 條 bullet；重點：Todo/task-tracking 工具（TaskCreate/Get/Update/List、TodoWrite）預設在 Opus 4.8／Sonnet 5／Fable 5／Mythos 5 等新模型上移除，需設 `CLAUDE_CODE_ENABLE_TODO_TOOLS=1` 才能恢復（★★★★，直接命中本 repo「beads (bd) 任務圖」段落引用的 harness TaskList 機制與本次 probe 任務所在 session 使用的模型 Sonnet 5——若未設該 env var，session 內即時清單可能已無法用 TaskList，需留意是否影響「session 內即時清單照用 harness TaskList」的既有慣例）；print mode 診斷新增 `[claude-code:unrecognized_model]` stderr 訊息，可用 `modelOverrides` 消音（★★★，直接命中 cc-vendor-bridge/model swap 派工——ccp-glm/ccp-gpt/ccp-bruce 這類非標準 model ID 派工會觸發此訊息）；修復 bundled skill alias（如 `/checkup`、`/review`）在有 user/project skill 同名 shadow 時於 `-p` mode 或掛 plugin/MCP 時誤報「Unknown command」（★，2026-08-15 實查本機無同名者：`~/.claude/commands/` 無 `review.md`／`checkup.md`，`~/.claude/skills/` 只有 `review-html`／`review-zh`／`bitbucket-pr-review`，名字都不同、結構上不會踩到；本 repo `.claude/` 底下只有 settings 與 hooks、零 command／skill）；修復 skill/command 參數代入時把值本身誤當 template marker 再展開（★★，命中 skill-creator/gate-authoring 撰寫紀律）；改進 `claude plugin validate` 對裸 `.claude/skills` 目錄檢查 SKILL.md frontmatter 解析錯誤（★★，命中 skill 生態撰寫紀律）；修復 MCP v2 連線對固定 timeout 關閉長連線的 server（如 serverless host）陷入無限重開 subscriptions/listen stream 迴圈（★★，命中本 repo 掛多個 MCP server 的可靠性）；新增 `CLAUDE_CODE_WEBFETCH_CACHE_TTL_MS` 可調整 WebFetch session URL cache TTL（預設維持 15 分鐘不變）（★★，命中 digest workflow 大量 WebFetch 使用場景）；回退 v2.1.232 對 Windows Cygwin-style symlink 與 input redirection（`< file`）的權限變更，稱窄化版本會在後續版本回歸（★★，直接更正前一版 baseline 標記為 ★★★ 的「Bash input redirection 權限檢查補齊」項目——原文「Reverted … for Cygwin-style symlinks **on Windows** and for input redirections (`< file`)」裡的 on Windows 只修飾 Cygwin symlink 那項；input redirection 的檢查 v2.1.232 本來就是全平台加的、這次也是**全平台回退**，macOS 上該檢查已經沒了）；其餘較不相關項目均標 ★（GitLab MR URL 支援 `--worktree`／`claude agents` view——未見使用者用 GitLab；`forward_user_identity` apps gateway 設定——企業 gateway per-user 計費場景不適用；Linux Bash tool memory cgroup 限制——Linux 專屬；修復 cloud session 在等待權限提示時環境關閉被誤標為 lost——未見使用者用 cloud session；修復 Notification hook 在 Claude Desktop/VS Code 下對權限提示不觸發——使用者主要走 CLI；修復 Linux 開 sandbox 時 idle session 偶爾佔滿一核 CPU——Linux 專屬；修復 Windows NT `\??\` 路徑繞過 UNC 驗證的 NTLM 憑證外洩風險——Windows 專屬；改進 `claude self-hosted-runner` session 啟動時間——未使用；改進 apps gateway 對 Vertex/Foundry/AWS Claude Platform 的錯誤轉發——未走這些 vendor 路徑；改進螢幕閱讀器模式 `/effort` 選擇器——無障礙功能不適用；GitHub app 設定提示改為不在 GitLab/Bitbucket origin remote 的 repo 顯示——本 repo 皆 GitHub origin；Windows auto mode 對一般 `cd && command > file` 重複卡權限確認的 2.1.232 regression 修復——Windows 專屬）。以上逐項對帳 `gh release view v2.1.233` 的 20 條 bullet、無漏項。升級指令：CC 具備自動更新，如需強制檢查可跑 `claude update`（2026-08-15 未實測本機 `claude --version`，只查 GitHub release API）
  - `v2.1.232 (2026-08-13T23:29:59Z)` — 2026-08-14 probe baseline（涵蓋 v2.1.231 + v2.1.232 兩版，距前一 baseline v2.1.229 共 2 個新 release；v2.1.231 距 v2.1.229 約 12 小時，重點：修復 MCP OAuth 登入在使用預先註冊 OAuth client 的伺服器（如 Slack）遇到 redirect URI 不匹配而失敗的問題（★★，命中 MCP 連線可靠性紀律，本 repo 掛有 `mcp__plugin_slack_slack__*` 系列工具，屬同一 MCP OAuth 機制的直接受益方）；v2.1.232 距 v2.1.231 約 15 小時，重點：Subagent forking 預設開啟——`subagent_type: \"fork\"` 的 subagent 現在會繼承完整對話與 prompt cache，互動 session 裡非隊友 agent 的 spawn 也改為預設在背景執行（★★★★，直接命中 dispatch-and-verify 紀律「~~fork~~ 已亡：CC v2.1.212 起 Agent tool 無此類型」段落——這條疑似代表官方重新開放/預設化 fork 機制，該紀律段落的現行性需要重新驗證，是本次 probe 對既有工作紀律影響最大的一條）；prompt 打 `@` 提及另一個 Claude session、Claude 會用 `SendMessage` 直接聯繫（★★★，命中多 agent 協作與本次任務正在用的 SendMessage deferred tool）；`SendMessage` 對唯一符合的裸名稱直接送達、不再先問 confirm（★★，同一協作機制精進）；同機互動 session 現在維持唯一名稱、撞名自動變體並提示（★★，命中多 session 協作命名衝突處理）；`/config` 新增「Dialog expiry」與「Messages from your other sessions」兩列（cross-session inbound accept/hold/refuse）（★★，命中 auto mode 安全紀律與跨 session 訊息授權邊界）；新增 GitLab token 家族 secret redaction + `glab` CLI 設定檔比照 `gh` 的 sandbox/credential-path 保護（★，未見使用者走 GitLab）；plugin marketplace 新增 GitLab 支援（★，未見使用者用 GitLab marketplace）；`additionalMarketplaces`/`allowedMarketplaces` 作為既有 key 的更友善別名（★，config 撰寫便利性、低相關）；enterprise policy 的 url 型 `blockedMarketplaces` 修復漏擋（★，org 政策場景不適用）；Gateway `desktop:` overlay 改接受 Desktop 全部設定並在啟動時驗證（★，未見使用者走 gateway desktop overlay）；Gateway 對 `managed.policies` 空 groups / 格式錯誤 `email_domain` 改為啟動時直接失敗、不再靜默誤授權（★★，命中 config-authoring「silent misconfig 應 fail-loud」精神，雖是 org gateway 場景但原則相通）；Fable 5 重新在 `/advisor` 對有 Fable 權限的組織開放、透過 `/model fable` 設定 usage-credits 同意（★★★，直接命中 Subagent 模型選擇「Fable 兩個政策入口」機制段落）；修復 PowerShell 權限繞過（★，Windows 專屬）；修復 Windows Git Bash 對 Cygwin-style symlink 的權限繞過（★，同上）；修復巢狀 git repo 會繼承上層目錄信任、現在每個 repo 各自要求信任確認（★★，命中「不可逆/破壞性動作」授權邊界紀律，本 repo 常用多層目錄/worktree 結構）；修復 MCP 連線在伺服器對 protocol-version probe 無回應或格式錯誤時卡滿 30 秒逾時（★★，命中 MCP 連線可靠性，本 repo 掛多個 MCP server）；修復 Remote Control 在 cloud session 內由 bridge 託管時繼承該 session 的 transcript 或憑證（★，未見使用者用 Remote Control bridge）；修復 Desktop/IDE 啟動的 Remote Control session 每次 resume 被當新 claude.ai session（★，未見使用者用 Remote Control）；修復 Remote Control session 閒置時對新連上 client 顯示不可達（★，同上）；修復 Remote Control bridge session 在 worker 重啟時未還原對話歷史（★，同上）；Remote Control resume 已刪除 session 時改起替代 session 而非顯示登入錯誤（v2.1.227 引入的 regression，現已修復）（★，同上不適用但記錄修復時序）；修復 Cloud gateway `/login` 在 managed settings 載入失敗時靜默結束（★，未見使用者走 cloud gateway login）；修復原生版 voice mode 卡在「listening…」（★，未見使用者用 voice mode）；修復 mTLS client 憑證輪替需重啟才生效（★，enterprise mTLS 場景不適用）；修復格式錯誤 AWS/Vertex region 值被拿去組請求 URL（★，未見使用者走 AWS/Vertex）；修復 Bedrock/Vertex/gateway 部署上 stream idle timeout 直接失敗、現在會嘗試恢復（★，同上場景，但屬 cc-vendor-bridge gateway 部署可能相關的可靠性補強）；修復內容大小 overlay 截斷文字渲染瑕疵（★，UX 細節）；修復長 shell 指令/agent 描述預覽在 emoji 中間截斷產生亂碼（★，UX 細節，本 repo 大量長 Bash 指令描述、屬低機率會撞到的顯示瑕疵修復）；修復啟動時並發寫入 `known_marketplaces.json` 可能靜默移除某個 plugin marketplace 的 race condition（★★，命中 skill/plugin 生態穩定性紀律）；修復 `/update` 與 `/tui` 在有跨重啟存活工作進行中時拒絕重啟（★★，命中長 session/session resume 可靠性紀律）；修復 SDK/remote session 的 usage-limit 提示建議不可用 slash command（★，未見使用者走 SDK/remote session）；修復互動式 `--advisor fable` 啟動的同意訊息教你在已結束的 session 裡跑 `/model fable`（★★，命中 Fable advisor 入口正確性，與上面 Fable 5 重新開放同一脈絡）；改進全螢幕串流——長 session 不再每次更新都重新 normalize 整段對話（★★★，直接命中長 session 效能/可靠性紀律，本 repo daily digest workflow 常跑長 session）；改進 managed settings 核准對話框——顯示 endpoint URL、telemetry 措辭更清楚、要求核准 sandbox binary override（`sandbox.bwrapPath`/`sandbox.socatPath`/`sandbox.ripgrep`）（★★，命中 config-authoring 與 sandbox 安全紀律）；`/feedback`、`/bug` 改為回應中途也能立即開啟（★，UX 細節）；`/plugin install plugin@marketplace` 改為先刷新該 marketplace 再裝（★★，命中 skill/plugin 生態使用習慣，降低新 plugin 手動步驟）；`/code-review` 在 high/xhigh/max effort 改為背景 agent 執行（★★★，直接命中日常 `/code-review`、`/pr-review`、`review-spec` 重度使用場景，高 effort review 不再擋住 session）；貼上與剪貼簿圖片改為非阻塞讀取（★★，命中長 session 效能可靠性）；Remote Control 網路短斷後改為持續重連約 30 分鐘（★，未見使用者用 Remote Control）；Remote Control resume 不再靜默搶走同機另一個仍持有它的 Claude Code（★，同上不適用）；agent panel 改版——已完成 subagent 立即隱藏並顯示 `/tasks` footer 提示、溢出指示移到左邊（★★，命中背景 subagent 監控 UX，本 repo 大量 fan-out workflow 依賴 agent panel 觀察進度）；Remote Control 終端明確顯示 session 被接管/結束/刪除的原因（★，同上不適用）；Bash 的 input redirection（`< file`）現在所有平台都比照參數寫法做權限檢查（★★★，直接命中 Bash 指令審核紀律，補上先前可能繞過 permission check 的路徑）；縮短 resume 已完成背景 agent 的顯示訊息（★，UX 細節）；Cowork session 不再內聯展開 user-scope memory 檔案裡的外部 `@`-import（★★，命中 auto memory / MEMORY.md cluster 引用機制相關脈絡，Cowork 場景本身使用者未見走）；強化共享 `/tmp` 上自動產生的跨 session 訊息 socket 目錄——預先埋放的 symlink 或他人目錄現在會被拒絕（★★★，命中安全紀律，直接補強本次任務用到的 cross-session messaging/SendMessage 機制本身的 local privilege escalation 防禦面）；強化 Linux 檔案系統 sandbox 防止受保護路徑被繞過（★，Linux 專屬、使用者 macOS）；`sandbox.ripgrep` 改為只認 user/managed/`--settings` 層級設定、project 層不能再覆寫（★★，命中 gate-authoring/sandbox 設定紀律）；移除建議建立自訂 subagent 的啟動提示與 `/powerup` tour 對應提醒（★，UX 細節，agent-authoring skill 仍是正式建 agent 的入口、不受影響）。以上逐項對帳 `gh release view v2.1.231`/`v2.1.232`，v2.1.231 1 條 bullet 無漏項、v2.1.232 49 條 bullet 無漏項。升級指令：CC 具備自動更新，本機 `claude --version` 2026-08-14 現查已是 `2.1.232 (Claude Code)`（無需手動操作）；如需強制檢查可跑 `claude update`
  - `v2.1.229 (2026-08-12T20:56:22Z)` — 2026-08-13 probe baseline（完整 changelog 與個人化解讀見當日 digest）
  - `v2.1.228 (2026-08-11T19:50:59Z)` — 2026-08-12 probe baseline（詳前次歷史鏈全文）

### ruanyf/weekly 新一期 watcher

- **Why**: 阮一峰《科技愛好者周刊》每週五出一期，內容近兩年高度集中在 AI 編程 / 模型經濟 / 軟體開發方法 / 工程師職涯，跟使用者 daily-topic-radar、interview-tour-2026、本機 AI 工具研究的選題重疊度極高（第 398 期頭條就是 OpenClaw 創辦人曬 Token 用量、Uber 燒爆 AI 預算、微軟棄用 Claude Code）。當成固定外部視角源，有新一期就在當天 digest 摘要+個人化評論。
- **Source(s)**: <https://github.com/ruanyf/weekly>（README.md 是全期索引、`docs/issue-N.md` 是各期正文）
- **How to fetch**: 用 authenticated `gh`（匿名 `curl` 打 GitHub API 會撞 rate limit 403）。取最新期號：`gh api repos/ruanyf/weekly/contents/README.md --jq '.content' | base64 -d | grep -oE 'docs/issue-[0-9]+\.md' | head -1 | grep -oE '[0-9]+'`。若 > Last seen，抓正文：`gh api repos/ruanyf/weekly/contents/docs/issue-<N>.md --jq '.content' | base64 -d`。
- **Hit signal**: 最新期號 > `Last seen` 的期號 → 有新一期。
- **Action on hit**: stage-2 digest 開「📰 科技愛好者週刊新一期」section、摘該期 12 個固定欄目（封面圖 / 本週話題社論 / 科技動態 / 文章 / 工具 / AI 相關 / 資源 / 圖片 / 文摘 / 言論 / 往年回顧）重點，標跟使用者 stack / 求職 / AI 工具研究對應的個人化評論。**正文裡的 GitHub repo 按 digest「🛠 GitHub 倉庫觀察」鐵律集中、不散落本段**。
- **Triggered by**: `stage-2-digest` only（**不在 06:45 daily probes 跑** — 週刊一週才一期、比 CC release 更低頻，daily 比對無差分價值徒增 gh call；digest 階段比對 Last seen 即可 catch）
- **Last seen**: 第 412 期；README 最新期號路徑 docs/issue-412.md；release commit 6ff7bd33「docs: release issue 412」，authored 與 committed 皆為 2026-09-11T00:20:28Z — 2026-09-11 probe baseline；README 現查最新期號路徑為 docs/issue-412.md，412 高於 PROBES.md entry 的 Last seen 411；docs/issue-412.md 的唯一 commit 為 6ff7bd33「docs: release issue 412」，author date 與 committer date 皆 2026-09-11T00:20:28Z；第 412 期標題為《科技爱好者周刊（第 412 期）：禁止 issue，只用 PR》；頭條記載 PHP 框架 Laravel 上週宣布新規定：禁止提交 issue、只能提交 Pull Request；頭條理由一：沒有了垃圾 issue——願意花時間開 PR 的人對問題更上心，機器人與騷擾者不願多花時間；頭條理由二：對維護者更方便——維護者從 PR 得到的資訊遠多於 issue，理解與解決問題投入時間更少；頭條理由三：沒有增加使用者的提交難度——不懂原始碼也能向 AI 描述問題讓 AI 生成 PR，AI 時代提交 PR 幾乎和提交 issue 一樣簡單（原提案寫成「難度不比 issue 高」，略強於原文的「幾乎一樣簡單」）；頭條理由四：禁止 issue 的根本原因是開源項目資源不足、應付不了激增的 issue；第二篇《史上最长的数学程序》記載 Anthropic 使用 Claude AI 完成費馬大定理的程式化證明；該篇記載研究團隊上週宣布 Claude AI 用 11 天把安德魯·懷爾斯的證明翻譯成 Lean 語言程式，懷爾斯的證明原文共 129 頁；該篇記載 Claude 在證明最終結果前先證明超過 3 萬個輔助定理、用到其中 2 萬 9 千多個、消耗數十億 token，最終代碼長達 1300 萬行；該篇記載所有代碼公布在 GitHub 的 anthropics/fermats-last-theorem；科技動態一：特斯拉在美國推出 Cybercab 出租車，本週宣布這些車對外出售，購車者與特斯拉對半分載客收入，另需另購 15000 美元自動駕駛功能包；科技動態二：戴森推出電動牙刷，刷頭帶攝影機與照明燈，會依攝影機拍到的景象自動判斷往哪裡噴水、加大清潔力度；科技動態三：哈佛大學工程學院為盲人與視障人士發明 AI 手杖，需搭配手機，由手機攝影機、GPS、運動與方向感測器擷取即時資料並結合導航軟體，手杖以嗶聲提示前進方向、走錯會發警報聲；文章欄：1981 年生的 Cloudflare 創始工程師李·霍洛威 36 歲罹患早發性癡呆、大腦萎縮，後來連話都說不全；文章欄：Hugging Face《開放模型 2026 年夏季述評》分析開放模型最新發展、可了解中國模型在整個 AI 市場的地位；文章欄：作者的桌上型電腦花 32 小時破解 1999 年頒發的 512 位元 RSA 密鑰，若用 GPU 集群約 1 小時可完成（目前密鑰至少 2048 位元）；文章欄：網頁自適應視口不再需要 initial-scale=1.0，只寫 width=device-width 即可；文章欄：遞迴堆疊過深會報錯、尾遞迴可解，但 JS 執行環境目前不支援尾遞迴，該文教因應寫法；文章欄：一位美國程式設計師在自家後院搭建簡易工作室，詳細記錄建設過程與成本拆解；工具欄共 10 條：FlyonUI、endlessh-go、PuTTYgen、gh gfm-preview、caddy-waf、一息、ctty、vet、Krep、Inbucket；資源欄共 3 條：黑客的 Linux 基礎知識筆記、DNSSEC 工作原理漫畫網站、Gorgeous GRUB 開機畫面主題收集；圖片欄為美國公路旁的奇特建築照片系列；文摘《在开始构建任何东西之前，问自己三个问题》：能否把產品介紹寫成一頁概要、核心技術能否與產品分離、產品有沒有一個核心特色；言論欄共 5 則；言論欄引陶哲軒：數學難題不可再生、好問題已稀缺，自動化工具解題並未增加人類的數學思維、損害未來數學發展；言論欄引《域名的恶意注册》：目前每五個新註冊的頂級域名就有一個是詐騙域名（原提案漏「註冊」二字）；本期正文提及的 GitHub repo 包含 shizunge/endlessh-go、thiagokokada/gh-gfm-preview、fabriziosalmi/caddy-waf、Defiabell/yixi、zsuroy/ctty、vet-run/vet、davidesantangelo/krep、ahegazy0/linux-basics-for-hackers-notes、Jacksaur/Gorgeous-GRUB、anthropics/fermats-last-theorem
- **Baseline history**（新→舊、由 writeback helper 自動維護）:
  - 第 411 期；release commit `d2ad275`「docs: release issue 411」2026-09-04T00:08:41Z；另有格式修正 commit `1a24606`「Fix formatting in issue-411.md」2026-09-04T02:46:07Z — 2026-09-07 probe baseline，下次 trigger = 第 412 期 — 2026-09-07 probe baseline；README 最新期號路徑為 `docs/issue-411.md`，411 高於 PROBES.md entry 的 Last seen 410。；第 411 期標題為《科技爱好者周刊（第 411 期）：OpenClaw 2.0 是一个缩影》。；頭條指 OpenClaw 本週發布 2.0 版，並回顧它今年春節一夜走紅、僅半年後已被遺忘。；頭條記載 OpenClaw 的代碼幾乎百分之百由 AI 生成，早期每天發一個版本，7 月起改為每月發一個版本。；頭條引述發布公告：8 月正式版（即 2.0 版）共有 933 位貢獻者、合併了 16000 個 PR。；頭條依官網「全職 9 人、兼職 26 人」推算每天要合併 500 多個 PR，作者結論是這些 PR 沒有經過代碼審查、都是 AI 合併的（作者自陳為猜想）。；頭條重申建議千萬不要在自己的工作電腦上運行 OpenClaw，而要在獨立的物理機、虛擬機或雲端運行。；第二篇《没人为你的堆栈辩护》介紹 SolidJS 創辦人 Ryan Carniato 的同名文章，指 AI 能重寫代碼、讓技術堆疊遷移成本與風險大降。；該文舉例：Cursor 主站與 Anthropic 文件站從 SolidJS 遷移到 React、Cognition 公司的營銷網站從 Astro 遷移到 Next.js、Bun 從 Zig 遷移到 Rust。；該文結論為「每一層最終都會變為當下最流行的解決方案」，並提問沒有多樣性與試錯的土壤時軟體生態的下一個創意從何而來。；科技動態：中國科學院完成地球與月亮之間的雙向高速雷射通訊，速度達上行 1.25Mbps、下行 100Mbps。；科技動態：韓國政府「計劃」向所有公民免費提供無限 Token，由韓國最大兩家電信商與 Kakao 三家承辦、每家配 512 個 NVIDIA B200 晶片，並規定至少 80% 使用量須導向韓國自己的大模型。；科技動態：NASA 本週發射主鏡直徑 2.4 米的南希·格雷斯·羅曼太空望遠鏡，並舉辦「認領像素」登記活動、認領者可拿數位證書。；科技動態：東京觀光雙層巴士推出車上回轉壽司服務，一層是廚房、二層靠窗一圈是傳送帶，每人 16000 日圓。；文章欄《为什么我的 Claude 额度十分钟就用完了》：作者 Claude Max 帳戶額度暴增消耗，查出某程式以叢集方式同時跑 1555 個 Claude Code 會話，並分享自製本地日誌分析工具 tare。；文章欄《WebMCP 简介》：Chrome 開始提供 WebMCP 介面，讓頁面用 JS 定義當前頁面的 MCP 介面供 AI 呼叫，取代截圖與從幾十個 div 猜按鈕位置。；文章欄其餘四篇為烏克蘭前線程式設計師整合多國武器系統、在 DigitalOcean 每月 4 美元跑 OpenBSD、用 `branch.sort` 讓 git branch 依提交時間排序、格式化剪貼簿 JSON 內容。；本期工具欄 10 條、資源欄 4 條、圖片欄 2 條、文摘 1 篇（《为什么自由软件的可用性往往很差》）、言論 4 則，均可自正文逐條枚舉。
  - 第 410 期；commit `03a832bb3ac64d5da6c30cec6cb6f7e85460d1c3`；committedAt 2026-08-28T00:02:40Z — 2026-08-28 probe baseline；README 最新期號路徑為 `docs/issue-410.md`。；最新期號 410 高於 PROBES.md entry 的 Last seen 409。；issue-410 的 commit SHA 是 `03a832bb3ac64d5da6c30cec6cb6f7e85460d1c3`。；issue-410 的 commit message 是 `docs: release issue 410`。；issue-410 的 commit authored 與 committed timestamp 都是 2026-08-28T00:02:40Z。；issue-410 標題為《科技爱好者周刊（第 410 期）：你需要知道的 AI 三种机制》。；頭條以參數、推理、聯網三種機制解釋 AI 為何能回答提問。；《動蕩的人工智能時代已到來》段落記載 AI 有風險且應減緩發展。；文章欄寫明智譜承認 Ox Alpha 是 GLM 5.3 Flash。；文章欄包含 Git worktree 使用詳解。；文章欄包含 AliExpress 使用 WebAudio 指紋識別。；工具欄包含 Vanilla OS 3、Termio、PicLite、Multi-Row Tabs 與 Minimalytics。；言論欄包含 AI 值班、AI 和司機式知識與 AI 相關可靠性事故。；其他欄目包含平面椅、虛擬化身演唱會、自行車助力器、城市道路與同質化網路。
  - 409（2026-08-21T00:10:15Z 發布；commit `docs: release issue 409`） — 2026-08-21 probe baseline，下次 trigger = 第 410 期。頭條《程序員的職業未來》— 以軟體工業化為主線預測 AI coding agents 長期成為流程、手工編碼限制增加，程式員主要工作轉為審查與測試 AI 產出的 code；另談把 SEO 變成「大模型優化」的提示詞玩笑。科技動態=小米 17 Ultra 把日食辨成月球地形 / Heart Aerospace 完成最大電動飛機原型首飛（27 分鐘、宣稱 200 公里）/ 餐廳平板強制小費 UI。文章=色影無忌關站 / Qwen 3.8 27B 預設 xhigh 過度思考 / Stripe 以 70 億美元收購 OpenRouter / 文字水印 / 關閉 SSH 22 埠 / PostgreSQL 與 SQLite 萬用論。工具=Beszel、microlighter、dgit、Bython、LibreDB Studio、shotsync、goku image-to-SVG、Gitu、Dbmate、lnav；資源=midipiano、中文版線性代數、Flexport Atlas、插頭插座博物館、Cure Dolly；文摘=AI 擠占所有討論與軟體 TEMU 化；言論=AI 紅皇后效應等。GitHub repo 按 digest 鐵律集中到「🛠 GitHub 倉庫觀察」。（正文含外部連結與未驗證文章敘述，digest 需按既有 URL/安全流程處理）
  - 408（**2026-08-14 發布**《你需要知道的 AI 缓存知识》；`gh api "repos/ruanyf/weekly/commits?path=docs/issue-408.md"` 唯一 commit 為 `2026-08-14T00:19:51Z docs: release issue 408`，週刊固定週五發布——2026-08-15 是**偵測日**不是發布日，前一版 baseline 誤記為發布日已更正） — 2026-08-15 probe baseline，下次 trigger = 第 409 期。頭條《你需要知道的 AI 缓存知识》— 拆解大模型「輸入 Token 緩存命中價」原理：緩存命中只收儲存費（DeepSeek 命中價是未命中的 1/50），各家緩存期限不同（Anthropic 5 分鐘、DeepSeek 10 分鐘、OpenAI 10-30 分鐘遞減失效、Google 1 小時內遞減失效），AI Agent 為保持緩存有效會定期發 keepalive 請求（舊建議 30 秒一次、新建議因緩存期限最短 5 分鐘已可放寬到 4 分鐘一次）——評：直接命中 claude-api skill 與 quota/billing cluster（Anthropic 快取定價機制）+ 本 repo daily-topic-analysis / deep-research-paced 等長跑 workflow 的成本控制（大量 subagent + 長對話 prompt caching 命中率影響成本）；「4 分鐘 keepalive」的具體數字可對照長 session 效能紀律。科技動態=帶藍牙識別的交通攝影機（車牌+藍牙 MAC+RFID+WiFi SSID 多重關聯推斷車主）/ 聲波滅火裝置（30-60Hz 低頻聲波、體積大且需近火源、難成規模應用）/ 無舷窗私人飛機（液晶屏播放外部攝影機畫面取代窗戶，減重降阻）/ 地球最長視線（吉爾吉斯斯坦皮克丹科瓦峰南望中國崑崙山脈 530 公里）。文章=常見任務執行工具對比（Bash/make/just/mise）/ 腳本上傳文件到 GitHub issue/PR 教程 / Canvas 取代 HTML 建網頁的利弊討論 / 安卓手機改造 Linux 伺服器（Termux）/ 電梯調度演算法科普。工具=Docker 官方沙箱工具（給 AI Agent 隔離運行環境，與 safe-trial/sandbox 撰寫紀律呼應）/ CertMate（自建 SSL 證書自動管理）/ crontab guru Dashboard（圖形化管理 Cron，可留意跟本 repo launchd 排程管理的對照）/ trash-cli（Linux 回收箱）/ LeePanel（本地面板管理遠程伺服器，純 SSH 無需裝東西）/ MarkCard Studio（Markdown 轉分享卡片）/ 日全食 3D 模擬器 / DBX（跨平台資料庫管理、可接 AI 與 MCP）。AI 相關=Cloudflare 開源內部 AI 門戶 Cloudflare OS（企業內 AI 代理+知識管理+自動化）/ 鄉音閣（文字轉方言語音）/ chat.md（VS Code 插件把 AI 對話變成 Markdown 文件、與本 repo digest workflow 產 markdown 報告的形式相呼應）/ Parse（自然語言網頁抓取轉 API，免費額度每分鐘 5 請求——與本 repo fetch-fallback.sh 多層抓取策略同類方向、可留意是否有互補路徑）。資源=昆蟲世界 3D 圖鑑 / 圖解分布式系統原理系列 / 吹口哨練習器。圖片=回形針收藏 / 古典雕像原始彩繪對比。文摘《荷蘭和中國》— 荷蘭人時間充裕但技術水平較低、中國節奏快但基礎設施更先進，兩種生活方式各有取捨，無絕對優劣。言論=Meta CTO 回應「AI 省下的時間應拿去做更多產品而非休假」/ 扎克伯格談模型蒸餾「應保護從任何可觀察事物學習的原則」——命中 llm-model-landscape 關於蒸餾/開源模型地緣政治張力的討論 / OpenCode 創辦人 Dax Raad 的本地硬體回本試算——⚠️ **週刊中譯翻錯、勿沿用**：第一手推文 <https://x.com/thdxr/status/2086599224674681242> 原文為「OpenCode Go 使用者平均每天 $1.14 跑 deepseek flash v4／同等的 dual DGX setup 要 $10,000／**24 年回本**／**at 10x the usage it takes 2.4 years**」，週刊把 10 倍條件配到 24 年（該頁留言區 2026-08-14 已有讀者指出）。另：DGX 是 NVIDIA 整機、非 Apple Silicon，apple-silicon-local-llm-serving wiki 全文查無 DGX／NVIDIA／回本試算，**不可**當該 wiki 的成本面佐證 / 亞里士多德「願意思考不接受的想法是受教育的標誌」。本期提及 GitHub repo 多條（fabriziosalmi/certmate、andreafrancia/trash-cli、gna1280072/LeePanel、pangxiaobin/MarkCardStudio、DophinL/solar-eclipse-2026-simulator、t8y2/dbx、cloudflare/cloudflare-os、rusiaaman/chat.md、xr843/insect-world 等）——按 digest「GitHub 倉庫觀察」鐵律應集中列出、不散落本段。前一 baseline `407（2026-08-07 發布《國家為什麼需要開源軟體？》）` — 2026-08-07 probe baseline，下次 trigger = 第 408 期。內容=頭條社論《國家為什麼需要開源軟體？》— 大模型 Kimi K3 開源引發美國政府考慮限制中國開源模型，隨後微軟、輝達、思科、Cloudflare 等美國科技公司聯署公開信《開放權重模型和美國 AI 領導力》，核心論點：AI 領導力取決於能否建立開放且滲透各領域的生態，限制開源模型最終只會讓 AI 落入少數大公司手中——評：命中 llm-model-landscape 研究脈絡（開源模型地緣政治張力）+ ai-coding-era-engineer-moat 研究（AI 集中在少數大廠手中的護城河討論）（其餘正文與前次歷史鏈全文詳見原 entry）

### Entry 範本

```
### <title — 簡短 probe 名>

- **Why**: <為什麼要 probe — context，之後 agent 整理結果時引用>
- **Source(s)**: <去哪查 — URL / repo / API endpoint>
- **How to fetch**: <agent 用什麼工具：`gh release view <owner>/<repo>` / WebFetch / `curl` JSON / Context7 / claude-in-chrome MCP>
- **Hit signal**: <什麼算「有新東西」— version > X / 上次 fetch 時間 / 特定 keyword 出現>
- **Action on hit**: <找到後做什麼 — inject 進當天 digest「外部訊號」段、標 priority、想抓細節 follow up>
- **Triggered by**: <`daily` / `stage-2-digest` / `both`> — `daily` 走 06:45 launchd probes wrapper；`stage-2-digest` 只在 digest pre-flight 由 agent 主動 fetch（適合 release 類低頻 / 高 cost / 需 user attention 的訊號、避免 daily spam）；`both` 兩邊都跑
- **Last seen**（optional）: <當前 baseline — 只放最新這一輪，空白代表第一次跑>
- **Baseline history**（自動維護、不用手寫）:
  - <前一輪 baseline>
  - <更前一輪 baseline>
```

## Resolved / Archived

_(probe 不再 relevant 時移到這、保留歷史、一年後刪)_

## 維護規則

- **Hit signal 必填**：沒寫判準 agent 不知道 baseline 在哪、會每天 report 同樣內容；最簡單寫「上次 fetch 時間 / version / hash」
- **How to fetch 寫具體命令或工具**：不要寫泛泛「查 GitHub」，寫 `gh release view <owner>/<repo> --json tagName,publishedAt`，agent 才能照跑
- **Triggered by 必填**：`daily` / `stage-2-digest` / `both` 三選一。`daily` 走 06:45 launchd 自動跑；`stage-2-digest` 只在使用者叫 stage-2 digest 時 agent 主動 fetch；`both` 兩邊都跑。**daily 跑時 agent 過濾掉 `stage-2-digest` only 的 entries**（讀 PROBES.md 時跳過、不 fetch 不 surface）；**digest 啟動時 agent 讀 PROBES.md 過濾 `stage-2-digest` + `both` entries 跑**（不論 daily 是否已跑過、digest 啟動時都跑一次拿即時 diff）。
- **Last seen 由 helper 寫、不由 agent 直接 Edit**：hit signal 達標時，probes agent 只回報兩個短欄位——`new_baseline`（這一輪新增的那一段）與 `expected_old_prefix`（當下 Last seen 值的**開頭字串、逐字照抄不做正規化**——helper 用 `startsWith` 比對：Last seen 是 `第 410 期；commit …` 就給 `第 410 期` 不是 `410`；是 `v2.1.250 (…)；v2.1.251 (…)` 就給 `v2.1.250`、不得挑同行後面較新的版號。2026-09-07 兩個 entry 同時因這兩種寫法失敗、當天 writeback 全滅）；workflow 交給 [`~/.claude/scripts/daily-topic-probes-writeback.mjs`](file:///Users/linhancheng/.claude/scripts/daily-topic-probes-writeback.mjs) 做 CAS 比對後 prepend：新的上位、舊的自動降級成 `Baseline history` 第一個子項。**agent 不得自己抄歷史鏈**——2026-08-25 之前 Last seen 是一行含完整歷史鏈的長字串（成長到 16,777 字元），writeback payload 因此膨脹到 70,660 base64 字元、被塞進 prompt 要 LLM 逐字轉錄，agent 只送出 9,013 字元就崩，整個 writeback 失效。改 helper 或欄位契約後必跑 `bash ~/.claude/scripts/daily-topic-probes-writeback.test.sh`（33 項）
- **Baseline history 只增不改**：helper 自動 prepend，不要手動編輯順序或刪項；要查舊 baseline 往下讀即可
- **空 PROBES.md 時 wrapper 跳過**：probes-daily.sh 偵測無 Active entry → 產極簡 report 「今日無 active probe」直接結束

## Output

寫到 `reports/local-analysis/{date}-probes.md`（gitignored，跟其他 4 channel local-analysis 同 dir）。
