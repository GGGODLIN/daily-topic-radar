# Probes — 主動拉外部訊號

> 手動 maintain 的「需要主動去查、不能等被動社群討論浮上來」的源。Agent 跑時讀此檔，對每個 active probe 跑對應工具 fetch、結果寫進 `reports/local-analysis/{date}-probes.md`（daily channel）或 surface 進 `reports/digest-{date}.html`（stage-2-digest channel）。
>
> 主動 = 直接拉外部 source（GitHub API / RSS / web）。被動比對既有 raw md 的東西放 `WATCH.md`。
>
> **觸發**（按 entry 的 `Triggered by` 欄位分流）：
> - **`daily`**：launchd `com.gggodlin.local-analysis-probes` 每日 06:45（在 4 channel 之後）→ 結果寫進 `reports/local-analysis/{date}-probes.md`
> - **`stage-2-digest`**：每次使用者叫 stage-2 digest 啟動時 agent 主動 fetch → surface 進 digest「🆕 ... 動態」section（不依賴 06:45 daily wrapper、避免低頻訊號 spam）
> - **`both`**：兩邊都跑
> - **手動**：使用者說「跑 probes」/「probes 一下」/「拉一下 probes」→ agent 讀 PROBES.md 對所有 entries 跑（不論 `Triggered by`）

## Active

### anthropics/claude-code release watcher

- **Why**: 使用者直接用 CC CLI（cmux + 多 worktree + ad-hoc session + launchd 周邊 routine）、上游每次 release 都可能影響 TUI 行為、hook 機制、daemon、MCP timeout、quota、deprecation。要在「每次叫 stage-2 digest 的當下」即時 fetch、從上次看的版本 diff 到最新、寫進 digest「🟣 Anthropic / Claude 動態」的「🆕 CC CLI release」小節（2026-06-20 起併區、原獨立段「🆕 CC CLI 動態」收為本區第一小節）。
- **Source(s)**: <https://github.com/anthropics/claude-code/releases>（via `gh release list anthropics/claude-code`）
- **How to fetch**: `gh release list --repo anthropics/claude-code --limit 10 --json tagName,publishedAt,name`，filter `publishedAt > Last seen`；對每個新版跑 `gh release view <tag> --repo anthropics/claude-code --json tagName,publishedAt,body` 拿 changelog 前 2000c。
- **Hit signal**: 最新 release `tagName` 比 `Last seen` 新 → 有新 release。
- **Action on hit**: stage-2 digest 寫進「🟣 Anthropic / Claude 動態」的「🆕 CC CLI release」小節、列每個新 release 的 tagName + publishedAt + body 重點 entry（按 hook / daemon / MCP / quota / deprecation / fast mode / agents 等分類）+ 每條標跟使用者 workflow 對應的個人化評論 + 顆星排序（★/★★/★★★）+ 升級指令。
- **Triggered by**: `stage-2-digest` only（**不在 06:45 daily probes 跑** — release 低頻、daily 跑會 spam；digest 階段才有差分價值）
- **Last seen**: `v2.1.216 (2026-07-20T22:14:00Z)` — 2026-07-21 probe baseline（★★★ 建議關注、worktree-isolated subagent git 隔離繞過修復 + `/rewind` symlink 安全修復 + workflow/排程 symlink 寫入外洩修復 + Bash 複合語句權限檢查修復 + 背景 agent resume 遺失 prompt/工具限制修復 + 長 session 訊息正規化二次方效能退化修復）：**v2.1.216 (2026-07-20T22:14:00Z) ★★★** 新增 `sandbox.filesystem.disabled` 設定，可只保留網路 egress 隔離、跳過檔案系統隔離（★★，命中 sandbox 設定微調需求）；修復長 session 中訊息正規化成本隨對話輪數呈二次成長、造成多秒卡頓與 resume 變慢的效能退化（★★★，直接命中長 session 慣性使用場景）；修復 auto mode 在 OAuth token 過期或輪替後、把「HTTP 401」分類器錯誤誤判成拒絕核准指令（★★★，命中 auto mode 使用場景）；修復 worktree-isolated subagent 能透過 `git -C`、`--git-dir`、`GIT_DIR`/`GIT_WORK_TREE` 把 git 操作重新導向共用 checkout、繞過隔離（★★★，直接命中 EnterWorktree/ExitWorktree 工具 + 重度 worktree 隔離工作安全性，前幾版才修過類似漏洞、這版是同系列補漏）；修復 worktree session 在工作目錄與所選 project 不符時，落到另一個 project 殘留 worktree 裡的問題（★★★，同樣命中 worktree 隔離工作安全性）；修復 `/rewind` 會透過 tracked path 上的 symlink 或 hard link 還原/刪除檔案，現在會跳過並回報跳過筆數（★★★，安全相關——直接命中本 repo 自身的 symlink 拓樸：`~/Desktop/projects/social-info` 是指到 `~/code/social-info` 的 symlink，若曾在該路徑下用 `/rewind`，舊版可能透過 symlink 誤還原/刪除到非預期位置）；修復 workflow 存檔與排程任務寫入會跟隨 `.claude` 底下的 symlink、可能導致寫入跑到 project 外（★★★，安全相關，命中 workflow script 撰寫 + CronCreate 排程工具 + 本專案 symlink 拓樸背景知識）；修復 Bash 指令權限檢查對 `&&` list 或否定式（negation）內含 redirect 的複合語句判斷不正確（★★★，安全相關、直接命中日常大量 Bash tool 呼叫）；修復 resume 的背景 agent session 會還原成預設 agent、遺失原本的 prompt 與工具限制（★★★，直接命中背景 agent 監控紀律 + subagent 模型/角色設定持久性）；修復背景 session 的 worktree 沒有 git repo 時無法刪除的問題（★★，命中背景 agent + worktree 清理習慣）；修復 `claude daemon stop --any` 可能透過過期的 legacy daemon lockfile 誤殺不相關 process（★★，daemon 穩定性）；修復高優先訊息在背景 subagent 啟動窗口送達時、該 subagent 會被誤取消（★★，命中背景 agent 監控 + fan-out 派工場景）；修復 Claude-in-Chrome 在 session OAuth token 缺少所需 scope 時、reconnect 陷入 403 迴圈（★★，命中 claude-in-chrome 重度使用）；修復 MCP re-authenticate 會在新登入成功前就先撤銷仍可用的舊 credential（★★，命中重度 MCP 掛載場景）；修復 skill / command 在 session 中被改動後、要重啟才會出現在 slash menu（★★，命中 skill-creator 撰寫 + 迭代 skill 的工作流）；修復帶 `name` frontmatter 的 plugin skill 在 slash-command 自動完成中遺失 plugin 前綴（★★，命中 slack / codex 等 plugin skill 使用）；`/context` 現在對超出 context window 的對話顯示明確警告、失敗的 `/compact` 改顯示為錯誤（★★，命中長 session context 管理習慣）；改善 `/fork` 確認訊息為單行、附新 session 名稱、`claude attach` id、以及「複本共用你的 checkout」提示（★★，命中 fork subagent 派工模式，延續前版 `/fork` 語意變更的後續 UI 調整）；更新內建 dataviz skill：重排預設圖表色票 + 修正四系列圖表誤建議直接標籤的說明（★★，命中 dataviz skill 使用）；背景 session 的 `/mcp` 與 `/install-github-app` 在無終端機附掛時改為在 agent view 停放「needs input」請求（★★，命中背景 agent 監控紀律）；其餘：AskUserQuestion 修復——即使使用者自由文字回答要求「等等」或「先解釋」，Claude 過去仍被告知繼續，現在自由文字回答改用中性措辭（★，使用者已 deny AskUserQuestion 走文字選項替代方案、此修復對本地設定影響小）、Claude Code on the web session 閒置數分鐘後重問同一問題並丟失回答的修復（★，不相關 web 版使用）、Bash 指令解析非 ASCII 字元對齊真實 shell word boundary 的修復（★★，泛用可靠度改善）、@-mention 在檔案修改型 hook 後靜默附加不到內容 / vim dot-repeat 修復 / statusline resume 時跑兩次 / resume-picker 失敗時掛住等多項修復（★★，命中 cc-statusline-widgets + vim mode + resume 使用習慣）、Esc-Esc 在長跑背景任務 session 的閒置 prompt 不開 rewind picker 的修復（★，UI 細節）、Ctrl+X 連按兩次刪不掉 agent list session、以及背景 worker 死掉後被刪的 session 又重新出現的修復（★★，命中背景 agent 監控）、GUI editor（`/memory`/`/plan`/`/keybindings`/Ctrl+G）開啟時終端機滑鼠與焦點殘影、`/memory` 不再等 editor 關閉的修復（★，UI 細節，但命中 `/memory` 使用習慣）、Windows 唯讀指令存取網路路徑未提示 permission / PowerShell tool 對含不可見 Unicode 字元指令的驗證修復（★，不相關平台）、fullscreen 對話框超出面板右緣 / `/config` 設定清單裁切鍵盤提示 footer / transcript-mode footer 在窄於 104 欄終端機換行等 UI 修復（★，UI 細節）、Prometheus metrics endpoint 輸出無效 `# UNIT` 行的修復（★，不相關 telemetry 場景）、telemetry 誤報 permission 拒絕（失敗的 permission-prompt request 不再算 user rejection、user interrupt 改報 user abort）（★，telemetry 細節）、PowerShell tool 對 `git`/`gh` 指令參數驗證改善（★，不相關平台）、`/ultrareview` diff-too-large 錯誤訊息改善（顯示上限/實測大小/最大差異檔案）與 `/code-review ultra` 空 diff 訊息改善（點名 base ref、建議明確帶 base）（★，未見使用者使用 `/ultrareview`，`/code-review` 使用但屬邊角訊息改善）、spend limit 調整 prompt 顯示伺服器拒絕原因（★，quota 追蹤場景邊角）、VSCode 混排英文/程式碼時 RTL 文字（阿拉伯文/希伯來文/波斯文）渲染順序錯誤的修復（★，不相關）、cloud session container 中途重啟時遺失進行中訊息、現在改為 resume 後重跑該輪而非卡住無回應（★，不相關 cloud session 使用場景）。前一 baseline `v2.1.215 (2026-07-19T02:56:01Z)` — 2026-07-20 probe baseline（★★★ 建議關注、Claude 不再自動主動觸發 `/verify` 與 `/code-review` skill、需明確用 slash command 呼叫才會跑）：**v2.1.215 (2026-07-19T02:56:01Z) ★★★** Claude 不再自動（on its own）執行 `/verify` 與 `/code-review` 這兩個 skill，必須明確輸入 `/verify` 或 `/code-review` slash command 才會觸發（★★★，直接命中 CLAUDE.md「skill 觸發條件命中時主動 invoke」工作紀律，以及 superpowers:verification-before-completion / code-review 系列先前倚賴的自動把關行為——這條 release 後這兩個 skill 不會在完成宣告前自動介入驗證，需要改成顯式呼叫，否則會漏掉這層品質閘）。前一 baseline `v2.1.214 (2026-07-18T01:20:30Z)` — 2026-07-19 probe baseline（★★★ 建議關注、`Edit(dir/**)` 單層 allow 規則誤判 nested 目錄範圍修復 + hook exit code 2 遇 stdout JSON schema 驗證失敗時未如預期擋下的修復 + zsh `[[ ]]` 變數 subscript/modifier 誤判為惰性文字的修復 + memory frontmatter 值遇行內 `#` 被靜默截斷的修復 + SessionStart hook fork session 現在回報 source `"fork"`（不再誤標 `"resume"`）+ 長時間 tool call 心跳提示 + `subagentStatusLine` 加 reasoning effort 欄位）：**v2.1.214 (2026-07-18T01:20:30Z) ★★★** 修復單層 `dir/**` allow 規則（如 `Edit(src/**)`）會誤把整棵樹裡任何位置的 `dir/` 都當成 `<cwd>/dir` 自動核准寫入、範圍比預期廣（★★★，直接命中使用者大量 permission allow 規則設定，過去可能誤放行非預期目錄的寫入）；同批修正：hook `if:` 條件的單層 `dir/**` 現在只比對 `<cwd>/dir`，要 any-depth 比對需明確寫 `**/dir/**`（`deny`/`ask` permission 規則維持 any-depth 不變）（★★★，直接命中 gate-authoring / hook 撰寫紀律，既有 hook 若用單層 `dir/**` 語法預期 any-depth 要重新檢查改寫）；修復 hook 回傳 exit code 2 時、若 hook 自己的 stdout JSON 未通過 schema 驗證，不會如文件所述擋下（★★★，直接命中 gate-authoring / hook 撰寫紀律——過去這是嚴重的 silent-fail：hook 想擋卻因輸出格式錯誤而放行）；修復 Bash 權限檢查把 zsh 在 `[[ ]]` 比較裡的變數 subscript/modifier 誤判為惰性文字、導致該類指令未被要求核准就直接跑（★★★，使用者 shell 是 zsh，直接命中日常 Bash tool 呼叫行為與安全紀律）；修復 memory 檔 frontmatter 值遇到行內 `#` 會被靜默截斷（★★★，直接命中 MEMORY.md / wiki frontmatter 撰寫紀律，過去若 frontmatter 值含 `#` 可能是過去資料損毀但未被察覺的根因之一）；SessionStart hook 在 session 以 `/fork` 建立時現在回報 source 為 `"fork"`、不再誤標成 `"resume"`（★★★，直接命中 gate-authoring hook 撰寫 + 前一 baseline 提到的 `/fork` 語意改變——若既有 hook 靠 source 欄位分辨 fork/resume 要重新檢查邏輯）；新增長時間執行的 tool call 週期性 progress heartbeat、不再靜默無回饋（★★★，直接命中重度 MCP 掛載場景——chrome-devtools / claude-in-chrome / semble 長呼叫現在有進度提示）；`subagentStatusLine` payload 新增 reasoning effort 欄位、自製 statusline 可顯示 subagent 的 model 與 effort（★★★，直接命中 cc-statusline-widgets 自製 wrapper + widget 架構）；修復排程任務（scheduled tasks）把自己設定好的 prompt 誤判為不可信輸入而拒絕執行（★★，命中 CronCreate/CronList 排程與 injection defense 邊界，過去排程本身的合法 prompt 可能被誤擋）；其餘背景 session 群組修復（均★★，命中背景 agent 監控紀律）：displaced 背景 daemon 在 shutdown 時刪掉後繼 daemon 的 control socket、導致下個 client 誤殺健康替代 daemon 的修復；用 `←` 或 `/background` 停放的背景 session 若閒置會讓 daemon 與 worker process 永遠不釋放的修復；已完成的背景 session 無法用 `claude rm` 或 agent view 移除的修復；從非 git 資料夾派送的背景 session 無法從 agents view 刪除的修復；重開已停止背景 session 時若 session store 內有不可讀資料夾會無法還原對話的修復；`/install-github-app` 與 `/mcp` 設定選單在 agent-view session 被誤擋、現在只在無終端機附掛的背景 session 才拒絕的修復；長時間執行 session 在 OAuth token 輪替後 feature flags 變舊資料的修復；MCP 在 prompts/resources refresh 時的暫時性錯誤會清空該 server slash commands/resources 的修復（★★，命中重度 MCP 掛載場景）；修復 stream-json 輸出在 exit 時對讀取較慢的 SDK/pipeline consumer 被截斷、exit drain 現在依排隊位元組數量縮放而非固定 2 秒上限（★★，命中 workflow tool headless 執行 + local-analysis 19-channel 大量輸出場景）；修復 session cost/token telemetry 在收到多個累積 `message_delta` frame 的串流上重複計算（★★，命中 cc-statusline-widgets 花費顯示準確度）；修復 Bash 權限檢查對超過 10,000 字元的長指令未強制要求核准的問題（★★，命中大量 heredoc / 長參數 Bash 呼叫場景）；修復 Bash 權限檢查對 file-descriptor redirect 形式（bash 解析與權限分析器不一致）未 fail closed 的問題（★★，安全相關、命中重度 Bash 使用）；修復 Bash 權限檢查誤自動核准某些可能執行不安全選項 / command substitution / 反斜線路徑的 `help`/`man` 指令（★★，安全相關）；修復 remote session 的 permission prompt 可能在本地確認對話框顯示前就先通過的問題（★★，命中背景/remote session 監控紀律）；新增 memory 檔 frontmatter 的 ISO `modified` timestamp（★★，命中 memory/wiki frontmatter 維護流程）；其餘：新增 EndConversation 工具（Claude 可對高度惡意使用者 / jailbreak 嘗試主動結束對話，仿 claude.ai 既有機制）（★，不相關個人使用場景）、OTel log event 加 `message.uuid`/`client_request_id`/`tool_source` + 新增 `CLAUDE_CODE_OTEL_CONTENT_MAX_LENGTH` 可調 60KB 截斷上限（★，不相關 telemetry 場景）、對帶 daemon-redirect flag（`--url`/`--connection`/`--identity`，含 Podman remote 模式）的 `docker` 指令加上 permission prompt（★，未見使用者有大量 docker 使用紀錄）、修復 GrowthBook feature 評估為 null 時的 crash + 修復格式錯誤的 flag payload 可能清空快取 feature flags（★，內部穩定性邊角案例）、修復 Bash tool 在 Linux 上 `pkill -f` pattern 意外匹配到 CLI 自己 process 導致整個 session 被殺的問題（★，不相關平台，但提醒：本地 run-daily.sh watchdog 也用 `pkill -f social_info` pattern，屬不同 process name 不會誤殺 CLI 本身）、修復 `--settings` 指向 device file 或多 GB 檔案時的無界記憶體成長、超過 2MiB 的 settings 檔現在啟動時就明確報錯（★，邊角案例）、修復 Windows 上 corporate proxy 後 streaming turn 出現「Socket is closed」失敗（★，不相關平台）、修復 PowerShell tool 多項 Windows-only 問題（子行程等待 stdin 導致 hang / 非 UTF-8 輸入 crash / 非 ASCII 輸出與 raw ANSI escape 造成的 crash / `where.exe`/`fc.exe`/`diff.exe` 回傳有效負值答案卻被誤判為錯誤 / Windows PowerShell 5.1 下 `>`/`>>` 寫出 UTF-16LE 檔案其他工具讀不了）（★，不相關平台，共 5 條）、修復 Windows PowerShell 5.1 session 的 permission-check bypass（★，不相關平台）、修復 Remote Control「session ready」推播通知在未明確啟用 Remote Control 的 session 也會誤觸發（★，不相關、未使用 Remote Control）、修復透過 `--settings` CLI flag 啟用的 plugin 不會載入的 regression（自 v2.1.181 起）（★，不確定使用頻率的邊角案例）、修復 `/ultrareview` 在沒有 merge base 的 repo 拒絕執行、現在會提議 review 全部 tracked files（★，未見使用者使用 `/ultrareview`）、修復 `claude update`/`claude doctor` 在 shell-config 路徑是資料夾時靜默 hang 住、`/status` 系統診斷區塊變空白（★，邊角案例、macOS 環境診斷場景可能命中但少見）、修復思考中出現的偽造「check your network」警告（★，UI 細節）、`claude rc` 在家目錄的 workspace-trust 錯誤訊息改善（提醒該處信任設定永不儲存、建議改到 project 目錄執行）（★，邊角）、修復 OTel log event 在 turn 的 async context 之外發出時遺失 interaction span trace context（★，不相關 telemetry）、對用 `-m`/`--magic-file` 或 `-f`/`--files-from` 的 `file` 指令改為需要 permission、不再自動核准（★，邊角案例）、keep-alive 連線池在偵測到 stale-connection 錯誤後改為停用、讓 retry 開新 socket（★，泛用可靠度改善，不特定命中場景）。前一 baseline `v2.1.212 (2026-07-17T00:26:27Z)` — 2026-07-17 probe baseline（★★★ 建議關注、`/fork` 行為改變（改建背景 session、原 in-session subagent 改名 `/subtask`）+ WebSearch/subagent spawn 雙上限防失控迴圈 + MCP 呼叫 >2 分鐘自動轉背景 + plan mode 檔案修改 Bash 指令繞過 permission prompt 修復 + worktree symlink 建檔外洩修復 + hook halt 遺失/誤報 user rejection 修復 + Task tool `mode` 參數棄用）：**v2.1.212 (2026-07-17T00:26:27Z) ★★★** `/fork` 行為改變：現在複製對話進新背景 session（`claude agents` 裡有自己一列），原本它啟動的 in-session subagent 改名為 `/subtask`（★★★，直接命中 fork subagent 派工模式——CLAUDE.md dispatch 規則寫「WebSearch/WebFetch/chrome nav > 2 次 → fork subagent（subagent_type: "fork"、繼承對話 context 與 model）」，這條規則引用的 /fork 語意已變、要留意改用 /subtask 才是原本行為）；新增 session-wide WebSearch 呼叫上限（預設 200 次、`CLAUDE_CODE_MAX_WEB_SEARCHES_PER_SESSION` 可調）防失控搜尋迴圈（★★★，命中使用者 11K query WebSearch 高頻使用 profile + deep-research 系列 skill 大量平行搜尋，長 research session 可能撞到上限）；新增 per-session subagent spawn 上限（預設 200、`CLAUDE_CODE_MAX_SUBAGENTS_PER_SESSION` 可調，`/clear` 重置）防失控派工迴圈（★★★，命中 local-analysis 19-channel fan-out + deep-research-paced 多 agent 編排場景）；MCP tool call 超過 2 分鐘自動移到背景執行（`CLAUDE_CODE_MCP_AUTO_BACKGROUND_MS` 可調/關閉）（★★★，命中重度 MCP 掛載場景——chrome-devtools/claude-in-chrome/semble 長呼叫不再卡住 session）；修復 plan mode 會在沒有 permission prompt 或 SDK `canUseTool` callback 下自動跑檔案修改 Bash 指令（如 `touch`/`rm`）（★★★，安全相關、直接命中 plan 執行流程 + 不可逆動作紀律）；修復 worktree creation 跟隨 repo-committed symlink at `.claude/worktrees` 可能在 repo 外建檔案（★★★，命中 EnterWorktree/ExitWorktree 工具 + worktree 隔離工作安全性）；修復 `continue:false` hook 的 halt 在 tool 失敗或 mid-stream 完成時被丟失、hook infra 錯誤被誤報為 user rejection（★★★，直接命中 gate-authoring / hook 撰寫紀律）；修復 `ExitWorktree` 在用 `--continue`/`--resume`（print/SDK mode）resume session 後失敗「no active EnterWorktree session」（★★★，直接命中 EnterWorktree/ExitWorktree 工具使用）；修復從 agent view 重開已停止背景 session 靜默失敗，現在會恢復或顯示原因並可強制重啟（★★★，直接命中背景 agent 監控紀律）；改善 web search / web fetch：修復 API overloaded 時把「API Error」文字誤當搜尋結果或頁面內容回傳、並對 529 / rate-limited 請求加 bounded backoff 重試（★★★，直接命中 fact-check research discipline——避免誤把錯誤訊息當內容處理進 digest）；deprecate Task tool 的 `mode` 參數（現在被忽略）、subagent 預設改繼承父 session 的 permission mode（★★★，直接命中 subagent-routing 派工紀律 + T2-routing-gate hook 機制，`mode` 參數失效意味著相關 hook/config 若有引用要重新檢查）；`/resume` 在 agent view 現在開 picker（含已從清單刪除的 sessions）、resume 選擇作為背景 session（★★，命中背景 agent 監控習慣）；新增 `claude auto-mode reset` 指令復原預設 auto-mode 設定（★★，命中 auto mode 使用場景）；改善 prompt caching：mid-conversation system block 現在能在 LLM gateway / custom base URL（Bedrock/Vertex/1P）後正常運作（★★，命中 cc-vendor-bridge 第三方 endpoint 派工場景）；session transcripts 現在記錄每則 assistant message 的 reasoning effort level（★★，命中 effort 控制習慣 + session 分析）；headless/SDK session 對 mid-turn `set_model` control request 立即套用新 model 不用等下一輪（★★，命中 model routing / subagent 模型切換場景）；`SendMessage` bodies 不再重複進 replayed history 與 tool results、降低 inter-agent messaging token 用量（★★，命中 agent teams / SendMessage 多 agent 協作場景）；agent view / `claude agents --json` 對等待 sandbox、MCP-input、managed-settings prompt 的 sessions 改顯示「Needs input」而非「Working」（★★，命中背景 agent 監控）；修復背景 agent cold-attach 立即顯示 transcript、不再空白等待（★★，命中背景 agent 監控）；修復 SIGTERM during running Bash tool（print/SDK mode）孤兒化 process tree，改為中止 turn、殺 tree、exit 143（★★，命中 headless/workflow 執行穩定性）；修復 workflow agent grid 對 mid-run 加入的 Remote Control clients 保持空白（★★，命中 workflow tool 使用）；修復 streaming-mode control requests 在 handler 完成前被標記完成、session restart 時可能遺失（★★，命中背景自動化穩定性）；修復 `/fork` 建立的背景 sessions 在 state write failure 後失去 live-parent protection（★★，命中 fork subagent 使用）；其餘：裸 `/btw` 改為重開最近一次交流的 side-question panel（★，命中 aside skill 但影響小）、修復 conversations with many images 誤報「Request too large」（★，邊角案例）、修復 shell mode（`!`）在路徑 autocomplete popup 開啟時不執行含檔案路徑的指令（★，UI 細節）、修復 auto-mode denial notifications 長理由被截斷 mid-emoji 顯示破字（★，UI 細節）、修復 Ctrl+J 在 agent view dispatch input 不插入換行（★，UI 細節）、`/ultrareview` 多項修復（接受 `#123`/`PR 123`/貼上的 PR URL、自動 fetch remote branch、`/clear` 後跳過 billing 確認、Claude Desktop「not a git repository」錯誤訊息改善）（★，不相關 review 工作流）、修復 hosted session 因 repo mTLS/CA/OAuth 設定啟動失敗（★，不相關企業設定）、修復 `/background`/`claude --bg` 在 Windows Group Policy 擋 PowerShell 5.1 時失敗（★，不相關平台）、OpenTelemetry HTTP export 相容性修復（Azure Monitor 411/400、OTLP trace_id/span_id 缺失）（★，不相關 telemetry 場景）、修復 agent teams 重複 idle 通知（★，不相關 agent teams 使用）、UI 細節修復（welcome banner 版面 / diff preview 窄版面遺失行號 / plan-approval footer 長路徑斷行）（★，UI 細節）、Enterprise `forceLoginMethod` 擴大生效範圍（★，不相關企業設定）、更正 2.1.200 release note 關於 tmux synchronized output 偵測（★，不相關）。前一 baseline `v2.1.211 (2026-07-15T23:02:35Z)` — 2026-07-16 probe baseline（★★★ 建議關注、auto mode 覆寫 hook ask 決策修復 + 背景 agent 被 kill 後自動復活與結果回報造假修復 + MEMORY.md 超限警告改排除 frontmatter/HTML comment + worktree 下 claude agents job 永久無法刪除修復 + subagent 明確 model override resume 後被還原修復）：**v2.1.211 (2026-07-15T23:02:35Z) ★★★** 修復 auto mode 覆寫 PreToolUse hook 對 unsandboxed Bash 下的 `ask` 決策、hook 給的 `ask` 現在會把決策下限鎖在「必須詢問」（★★★，直接命中 gate-authoring / hook 撰寫紀律 + auto mode 使用場景）；修復被使用者手動 kill 的背景 agent 會自動復活、且復活後從舊 session 重跑過期 prompt 的問題（★★★，直接命中背景 agent + workflow-monitor/session-monitor 監控習慣）；改善背景 agent 結果回報——Claude 現在回報仍在跑的 agent 真實狀態、等待真正完成而非捏造結果（★★★，直接命中背景 agent 監控紀律 + 宣稱與證據層級對位規則）；Memory 寫入的「超限警告」現在只量測實際載入內容、排除 frontmatter 與 HTML comment（★★★，直接命中使用者 25KB/200 行 MEMORY.md cap 紀律 + cluster 化維護流程）；修復 `claude agents` job 在 git 已不認得其 worktree 時永久無法刪除的問題，現在會顯示拒絕刪除的原因而非靜默重新出現（★★★，直接命中 EnterWorktree/ExitWorktree 工具 + 重度 worktree 隔離工作）；修復以明確 model override 產生的 subagent 在 resume 或收到後續訊息時會還原成父 session model 的問題（★★★，直接命中 subagent 模型選擇紀律——確定任務指定 sonnet 不該被還原成 main 的模型）；修復 `/clear` 沒有重置 session 花費計數器、statusline 現在 `/clear` 後費用會歸零重新計算（★★，命中 cc-statusline-widgets 自製 wrapper 場景）；修復多個平行 CC session 共用同一 credential store 時、wake-from-sleep 後會全部同時登出的問題（★★，命中多 session / 雙帳號並行使用習慣）；修復 plugin 提供的 MCP server 在閒置 web session 甦醒後不會重新連線、導致 MCP 呼叫失敗直到下一則訊息的問題（★★，命中重度 MCP 掛載場景）；修復 background job 掛在 LLM gateway auth（`ANTHROPIC_AUTH_TOKEN` + `ANTHROPIC_BASE_URL`）下、daemon 重新 spawn 後會誤判「Not logged in」的問題（★★，命中 cc-vendor-bridge 第三方 endpoint 派工場景）；修復排程（routine）沒有設定 schedule 時、下次執行時間顯示為西元 1 年的問題（★★，命中 CronCreate/CronList 排程工具使用）；「always allow」permission 規則改存到 repo root，讓在 git worktree 裡核准的規則能跨 session / worktree 持續生效（★★，命中重度 worktree 隔離工作 + permission 規則設定）；新增 `--forward-subagent-text` flag / `CLAUDE_CODE_FORWARD_SUBAGENT_TEXT` 環境變數，讓 stream-json 輸出包含 subagent 文字與 thinking（★★，命中 workflow tool / headless 執行 debug 場景）；修復 permission preview 轉發到 chat channel 時沒有中和 bidirectional-override / zero-width / 形似引號字元、導致 tool input 能視覺竄改核准訊息（★★，安全相關、命中 critical_injection_defense 規則）；其餘：nested `.claude/rules/*.md` 檔在 setting sources 排除 project settings 時仍載入的修復（★★，命中 config authoring 紀律）、Claude in Chrome extension 已啟用但 Chrome 未開啟時的啟動掛起修復（★★，命中 claude-in-chrome 重度使用）、剛停止的背景 session 從 agents view 重開會在同一 session id 下開出空白對話的修復（★★，命中背景 agent 監控）、`/loop` 用過一次後把 session 從 `/resume` 隱藏的修復（★★，命中 /loop skill 使用）、Vertex/Bedrock 啟動時誤打預設 Opus model 並印出多餘 fallback 提示的修復（★，不相關平台）、file upload 驗證修復（DOS device suffix / 多重 hard link）（★，邊角案例）、Claude in Chrome 遠端與 CLI session 檔案上傳修復（★，命中 claude-in-chrome MCP 使用）、輸入變成「?」被靜默吞掉並切換 shortcuts panel 的修復（★，UI 細節）、300ms 延遲揭露 async 內容（Settings tabs / Stats / diff views）的修復（★，UI 細節）、螢幕報讀使用者在 `/terminal-setup` 後失去終端機提示音的修復（★，不相關）、Windows headless print-mode session 在 stdin 不可讀時 crash 或靜默結束的修復（★，不相關平台）、背景 session 標題顯示 naming model 拒答文字（當 prompt 含連結時）的修復（★，邊角案例）、Windows 上 Claude in Chrome 設定頁開不出來的修復（★，不相關平台）、終端機版面配置與渲染效能改善（★，泛用改善）、Windows 上同步 skill/plugin 目錄命名加固 + `/clear` 後 CCR web fetch/search proxy 仍正常運作（★，部分不相關平台）、整數環境變數（timeout / token budget / retry count）支援科學記號與 `1e6`/`64_000` 這類數字分隔寫法（★，env var 設定細節）、文件連結更新（★，不相關）、`/usage-credits` 改為送出請求前先詢問組織 admin 確認（★，不相關個人使用場景）、Vim mode `s`/`S` 在 NORMAL mode 也能用（★，不相關非 vim 使用者）、VSCode Remote Control banner 說明文字更新（★，不相關）、Claude in Chrome 加固檔案上傳路徑驗證（★，安全但邊角）、Claude in Chrome `save_to_disk` 截圖動作現在真的寫檔並回傳路徑（★，命中 claude-in-chrome 截圖使用）、Bedrock/Vertex/Mantle/Foundry prompt-caching regression 修復（尾端 system context block 被誤計為新 token）（★，不相關平台）。前一 baseline `v2.1.210 (2026-07-14T23:45:25Z)` — 2026-07-15 probe baseline（★★★ 建議關注、hook callback timeout 誤報為 user rejection 導致 unattended session 卡住修復 + worktree isolation subagent 越界跑 git-mutating 指令修復 + killed background session 永久 git worktree lock 修復 + MEMORY.md 超限靜默截斷改明確報錯 + Agent tool 加固 indirect prompt injection 防禦 + Grep content mode 分頁誤報修復）：**v2.1.210 (2026-07-14T23:45:25Z) ★★★** 修復 hook callback timeout 被誤報為 user rejection、導致 unattended session 停下等待的問題（★★★，直接命中 goal-gen / session-monitor 無人值守自動化場景）；修復 `isolation: 'worktree'` subagent 能對主 repo checkout（而非自己隔離的 worktree）跑 git-mutating 指令的問題（★★★，直接命中 EnterWorktree/ExitWorktree 工具 + 重度 worktree 隔離工作）；修復被 kill 的背景 session 留下永久 `git worktree lock`，periodic sweep 現在會釋放 owning process 已消失的 lock（★★★，命中背景 agent + worktree 清理習慣）；Memory 寫入若讓 MEMORY.md index 超過 read limit，現在明確報錯而非靜默截斷（★★★，直接命中使用者 25KB/200 行 cap 紀律 + cluster 化維護流程）；加固 Agent tool 對抗透過 subagent 讀取內容間接注入的 prompt injection（★★★，命中 critical_injection_defense 規則）；修復 Grep content mode 在分頁超過結果末尾時誤報「No matches found」（★★，命中「修改後用 grep 驗證」工作紀律）；修復 `ultracode` keyword opt-in 在 webhook payload / relayed PR comment 等非人類輸入上誤觸發（★★，安全相關）；修復 skill/command 裡未匹配的 `$1`/`$2` 佔位符被靜默清除、現在原樣保留（★★，命中 skill 撰寫工作）；修復 plan approval 沒有 edit 卻被標「(edited by user)」、覆寫掉 plan file 的問題（★★，命中 plan 執行流程）；修復 plugin 提供的 MCP server 在 MCP re-sync 時被誤拆除的問題（★★，命中重度 MCP 掛載場景）；改善 auto mode：permission classifier 現在對 external session 預設用 Sonnet 5、session 第一次 request 驗證後鎖定（★★，命中 auto mode 使用）；`claude attach` 修復「job not found」/「agent is still starting」錯誤，改為等待 daemon 穩定後才 attach（★★，命中背景 agent 監控）；修復背景 worker 在 client 重置連線時 crash-looping（★★，命中背景自動化穩定性）；修復 `claude agents --effort ultracode` 沒有傳到 dispatched session 的問題（★★，命中 effort 控制習慣）；新增對 `Write(path)` / `NotebookEdit(path)` / `Glob(path)` permission 規則的啟動警告（建議改用 `Edit(path)` / `Read(path)`）（★★，命中 permission 規則設定）；其餘：collapsed tool summary 加即時 elapsed-time 計時器（★，UI 細節）、paste marker 洩漏進外部編輯器修復（★，不常用）、numeric bigint tool renderer crash 修復（★，邊角案例）、`cd` 移到背景後工作目錄狀態顯示修正（★★，命中 Bash run_in_background 使用）、`/doctor` 在 Bedrock/Vertex/Foundry 跳過 auto-mode 提案的修復（★，不相關平台）、SDK MCP server initialize 延遲連線修復（★，SDK 場景）、agents dashboard 幾項 UI 修復（★，UI 細節）、dataviz skill 色彩驗證改良（★，命中 dataviz skill）、screen reader 模式朗讀 permission mode 變更（★，不相關）、Fable advisor 暫時不可用提示（★，不相關）。**v2.1.209 (2026-07-14T06:36:28Z) ★★**：修復 `/model` 等 dialog 在 `claude agents` 背景 session 中被擋住的問題（revert 一個過寬的 guard）（★★，命中背景 agent 使用時的互動操作）。前一 baseline `v2.1.208 (2026-07-14T01:10:42Z)` — 2026-07-14 probe baseline（★★★ 建議關注、長 context session 誤判 100% used 修復 + Edit/Read/Grep/Glob 多項修復 + 大量 permission 規則延遲修復 + worktree Ctrl+X 安全修復 + rm -rf compound command 安全閘補齊 + session transcript 79 倍縮減）：**v2.1.208 (2026-07-14T01:10:42Z) ★★★** 修復 background agent 收到 reply 但 session 卡住無法送達時遺失文字的問題，改為 session 重啟後補送（★★，命中 background agent + workflow-monitor 監控習慣）；修復更新後背景 daemon 用舊 binary 啟動 worker 導致新版 worker 跑在舊 binary 上的問題（★★，命中 daemon/background session 穩定性）；修復 context window（與 auto-compact 指示器）在 CLI 自動更新後短暫重置回 200k、resume 長 context session 時誤判「100% context used」的問題（★★★，直接命中長 context session 慣性使用場景）；修復 supervised/background session 在伺服器以 GOAWAY 關閉 HTTP/2 連線時 request 進行中會 crash 的問題（★★，命中背景 session 可靠度）；修復 `claude -p` 輸出大型 response 時 stream-json/JSON 被截斷、result message 遺失的問題（★★，命中 workflow tool headless 執行）；修復 headless stream-json session 收到非字串 `set_model` payload 時永久 hang 住的問題（改回錯誤訊息）（★★，命中背景自動化 routine）；Edit tool 修復檔案被讀取後又被外部修改、但目標文字仍唯一時仍能成功編輯（★★★，直接命中日常大量 Edit 呼叫）；修復 Read 誤報空檔案為「shorter than offset」、Grep 對無效 regex 靜默回報「No files found」、Grep count mode 分頁時低估總數、Glob 遇路徑含 null byte 時 crash 不清楚（★★★，直接命中「修改後用 grep 驗證」工作紀律）；修復多個長時間 session 的 memory leak：MCP stdio server stderr 累積上限 64MB、LSP 文件改 50 份 LRU 上限、async hook output 背景化後仍殘留、headless/SDK session 大型 tool-result payload 無界成長（★★★，直接命中重度 hook/MCP 撰寫 + 長 session 使用）；修復大量 permission deny/ask 規則下每回合多秒延遲，改為規則比對器一次編譯快取（★★★，直接命中使用者大量 deny/ask 規則設定 + T2-routing-gate hook）；MCP 工具密集場景下每次 tool call CPU 開銷降低（快取 tool-pool 組裝，最高 7 倍加速）（★★★，命中大量 MCP server 掛載場景）；session transcript 大小縮減（edit 密集 session 最高 79 倍）+ 修剪被取代的 file-history 備份控制 checkpoint 磁碟用量（★★★，命中長 session + edit 密集工作模式）；resume 含背景 agent 或 fork 的大型對話時記憶體使用降低（★★，命中 fork subagent 使用）；已完成的背景 agent 會留在 `/tasks` 直到清理、不再消失（★★，命中背景 agent 監控）；attach 已停止的背景 agent 現在立即顯示 transcript、不再顯示空白「Session is starting」（★★，命中背景 agent 監控）；Agent view Ctrl+X 改為只刪除 renamed-branch worktree、絕不誤刪未 push 的 commit（★★★，直接命中重度 worktree 使用 + EnterWorktree/ExitWorktree 工具）；含 `$(…)`/backtick/`<(…)` 的 compound command 中的災難性刪除（如 `rm -rf ~`）現在在 `--dangerously-skip-permissions` 與 auto mode 下也會提示、與一般形式一致（★★★，直接命中「不可逆 / 破壞性動作」安全紀律）；`/release-notes`「Show all」不再把整份 changelog 注入 context（★★，命中本次 probe 這類 release-note 追蹤習慣）；workflow 存檔對話框改顯示 `CLAUDE_CONFIG_DIR` 而非固定 `~/.claude/workflows/`（★★，命中 workflow script 撰寫）；`/usage` 改為 rate-limited 時顯示「as of」的最後已知數據、不再報錯（★★，命中 quota 追蹤習慣）；Agent tool 在 subagent `tools` 清單解析為空時改回傳明確錯誤（★★，命中 subagent-dispatch 重度使用）；`apiKeyHelper` script 失敗不再被隱藏在 10 次靜默重試後的泛用 401 錯誤後面，3 次內就顯示 script 本身的錯誤（★★，命中第三方 vendor endpoint 撰寫）；其餘：新增 screen reader 模式（`--ax-screen-reader`）（★，不相關）、`vimInsertModeRemaps` 兩鍵 insert-mode remap（★，不相關非 vim 使用者）、`CLAUDE_CODE_PROCESS_WRAPPER` 企業 launcher wrapper（★，不相關個人使用）、多選選單滑鼠點擊支援（★，UI 細節）、fast mode 切換模型後失效的修復（★，不確定使用頻率）、`CLAUDE_CODE_MAX_OUTPUT_TOKENS` 科學記號被裁成 1 的修復（★，env var 設定細節）、超過 200 行的大型 markdown table 不再拖慢渲染（★★，命中長文件 review 場景）、Bedrock 相關修復（★，不相關 AWS 平台）。前一 baseline `v2.1.207 (2026-07-11T00:52:10Z)` — 2026-07-11 probe baseline（★★★ 建議關注、終端串流長 list/table/code block 凍結卡頓修復 + Deep research Fetch 階段 agent hostname 顯示修復 + plugin/hook/MCP headersHelper shell-injection 安全修復）；歷史鏈 v2.1.206 / v2.1.205 / v2.1.204 / v2.1.203 / v2.1.202 / v2.1.201 / v2.1.200 / v2.1.199 / v2.1.198 / v2.1.197 / v2.1.196（MCP per-server request_timeout_ms 修復 + `/doctor` CLAUDE.md 精簡檢查 / 背景 notification 防偽造 approval + auto mode rm -rf 安全閘 + verify skill 重複 rewrite 修復 / Sonnet 5 預設 + 1M context / Claude in Chrome GA / built-in Explore 改繼承主 session model / hook matcher 精確比對修復 / subagent 失敗如實回報等，見先前 baseline）

### ruanyf/weekly 新一期 watcher

- **Why**: 阮一峰《科技愛好者周刊》每週五出一期，內容近兩年高度集中在 AI 編程 / 模型經濟 / 軟體開發方法 / 工程師職涯，跟使用者 daily-topic-radar、interview-tour-2026、本機 AI 工具研究的選題重疊度極高（第 398 期頭條就是 OpenClaw 創辦人曬 Token 用量、Uber 燒爆 AI 預算、微軟棄用 Claude Code）。當成固定外部視角源，有新一期就在當天 digest 摘要+個人化評論。
- **Source(s)**: <https://github.com/ruanyf/weekly>（README.md 是全期索引、`docs/issue-N.md` 是各期正文）
- **How to fetch**: 用 authenticated `gh`（匿名 `curl` 打 GitHub API 會撞 rate limit 403）。取最新期號：`gh api repos/ruanyf/weekly/contents/README.md --jq '.content' | base64 -d | grep -oE 'docs/issue-[0-9]+\.md' | head -1 | grep -oE '[0-9]+'`。若 > Last seen，抓正文：`gh api repos/ruanyf/weekly/contents/docs/issue-<N>.md --jq '.content' | base64 -d`。
- **Hit signal**: 最新期號 > `Last seen` 的期號 → 有新一期。
- **Action on hit**: stage-2 digest 開「📰 科技愛好者週刊新一期」section、摘該期 12 個固定欄目（封面圖 / 本週話題社論 / 科技動態 / 文章 / 工具 / AI 相關 / 資源 / 圖片 / 文摘 / 言論 / 往年回顧）重點，標跟使用者 stack / 求職 / AI 工具研究對應的個人化評論。**正文裡的 GitHub repo 按 digest「🛠 GitHub 倉庫觀察」鐵律集中、不散落本段**。
- **Triggered by**: `stage-2-digest` only（**不在 06:45 daily probes 跑** — 週刊一週才一期、比 CC release 更低頻，daily 比對無差分價值徒增 gh call；digest 階段比對 Last seen 即可 catch）
- **Last seen**: `404（2026-07-17 发布《你需要知道的 AI 内存知识》）` — 2026-07-17 probe baseline，下次 trigger = 第 405 期。内容=头条《你需要知道的 AI 内存知识》— 本地跑 AI 模型选硬件的两难：独立显卡（RTX 5090，32GB 显存/104.8TFLOPS FP32/1792GB/s 带宽）算力强但显存太小，70B 参数模型 4bit 量化需 32.6GB 已超显存上限跑不动；板载统一内存 mini PC（AMD Strix Halo Ryzen AI Max+ 395，128GB 内存/14.8TFLOPS/256GB 带宽）容量够但带宽仅显卡 1/7，40GB 模型每秒理论只能读 6 次即 6 token/s、龟速；MoE 架构（Qwen3-30B-A3B 仅激活 3B 参数）让统一内存机型每秒可生成 100+ token 变得可用；另一缺点是板载算力慢导致长提示词处理动辄几十秒到几分钟；结论：现阶段本地跑大模型无论独显或统一内存都有缺陷，只能跑中等规模 MoE 模型且提示词不能过长——与使用者 apple-silicon-local-llm-serving wiki（M5 Pro 48GB 本地 LLM agentic serving、dual-ceiling 物理 RAM+cognitive protocol、本地 27-35B 串 CC daily-driver 死路结论）高度呼应，可交叉引用验证。次条《布尔变量如何起名》— 四前缀 is-/has-/can-/should- 起名法 + 永远不用否定词命名布尔变量。科技动态=OpenAI 键盘（Jony Ive 设计、230美元快捷键批准/否决 AI 代理操作、RGB 灯显示代理状态，评价毫无新意）/ 天问二号抵达小行星 2016HO3 传回照片（20米不规则形状碎片、下一步采样返回）/ 荷兰设计师风力编织机（阳台风力驱动毛线编织围巾）。文章=机器学习博士求职指南（DeepMind 入职回顾）/ 使用 DNS 通过 ACME 认证（HTTPS 证书域名验证）/ Go 语言的困境（论前途因慢语言变快、难语言变易而被中间地带挤压）/ JavaScript 哈希算法速度比较（MD5 vs SHA256）/ 终端控制字符一览表（33 个命令行控制符快捷键图）/ 理查德·费曼加入创业公司回忆录。工具=WhatCable（macOS 顶部菜单栏 USB-C 电缆特征检测）/ amber（简化 Bash 语法新语言、编译成 Bash）/ Ant（轻量 JS/TS 运行时、二进制仅 8MB）/ 屏译（开源安卓实时屏幕翻译）/ TurboOCR（GPU 高速文字识别）+ light-ocr（纯本地 OCR 库 JS/C++ API）/ Visprex（CSV 数据自动可视化）/ Loro（开源 CRDT 多人实时同步算法库）/ File Wizard（自托管文件格式转换+图像 OCR+音频转文字）。AI 相关=Oh My HuggingFace（非官方 HF 跨平台客户端）/ pi-auto-approval（Pi agent 插件、自动批准低风险权限确认、高风险仍交用户——与使用者 auto mode + permission classifier 关注点呼应）/ GPT Crawler（网站内容抓取生成 ChatGPT 聊天机器人）/ Tokenwiz（模仿 OpenAI tokenization 开源实现）。资源=Goto Onion（Tor 暗网网关）/ Fading Maize（AI 将 2001 年乐队录音改编成 2026 风格对比聆听）/ 迷宫算法收藏站。图片=法罗群岛海底环岛（大西洋海底隧道分岔处环岛、水母造型警示）。文摘《AI 本应节省时间，减少工作量》— 加州大学调查发现 AI 反而增加工作量+职业倦怠，原因：(1) 工作职责范围扩大（vibe coding 需工程师审查非程序员代码）(2) 工作/生活界限模糊（休息时间发提示词跑任务）(3) 多任务处理激增 (4) 自我强化循环；解决方法=重要决定前暂停 + 减少上下文切换 + 预留真人际交往时间——与使用者 mission-critical infra 不碰 hobby toolkit 工作风格研究 + AI career landscape 研究直接呼应。言论=霍金「如果你是太阳我就是黑洞」/《奇点越来越近了》「AI 模型世界像城市有五个企业总部区域+一个中国城」/《AI 是一个糟糕的工具》「提示工程完全是骗局、AI 是机器实现的相信我兄弟」/《为什么便利总是胜出》用面包机三年只用两次类比即使 AI 能轻松生成代码 SaaS 仍不会消亡。本期提及 GitHub repo 数条（darrylmorley/whatcable、ciddwd/overlay-translator、arcships/light-ocr、oh-my-hf/ohmyhf、Europa2061/pi-auto-approval、BuilderIO/gpt-crawler、1rgs/tokenwiz 等）——按 digest「GitHub 倉庫觀察」鐵律應集中列出、不散落本段。前一 baseline `403（2026-07-10 发布《为什么 Dropbox 不成功》）` — 2026-07-10 probe baseline，下次 trigger = 第 404 期。内容=头条《为什么 Dropbox 不成功》— Dropbox 定位 C 端消费者工具而非企业生产力工具是根本错误（消费者只喜欢消磨时间不喜欢节省时间，只有企业才会为提高效率的软件付费），与使用者 mission-critical infra 不碰 hobby toolkit / AI coding era engineer moat 研究直接呼应；《AI 的成本超过了工程师》— Anthropic 员工计算支出是工资总额 2.3 倍（人均 51.5 万美元 AI 费用 vs 22.4 万年薪）、前1%美国软件公司人均 AI 费用均值 8.9 万/中位数 13.7 万（占工资 40%-230%），若产出不能同等增长 AI 效益为负（与 AI career landscape 研究直接呼应）；科技动态=国内无屏幕相机（透明取景窗 65g 199元）/ 英伟达向创业公司提供算力后还款（类消费贷款）/ 布朗大学 AI 作弊丑闻（闭卷考后平均分 96→48、18名学生退课）/ 欧盟 7/7 起新车强制驾驶员面部监控摄像头；文章=Elm 1.0 铁树开花（上版2019、启发 React 单向数据流）/ 面试题找出中位数 / TypeScript 7.0 正式发布（引擎 JS→Go 速度提升10倍）/ 阅读代码前先跑5条 git 命令 / x402 协议（网页付款才看内容）/ CSS 锚点定位 API（所有主流浏览器已支持）；工具栏=Vite+（统一 JS 工具单二进制，类似 meow）/ Davit（macOS Docker 容器桌面管理）/ Flint（微软+人大可视化图表引擎给 AI 调用，支持 Vega-Lite/ECharts/Chart.js）/ LaTeXSnipper（手写数学公式转 LaTeX）/ OnlyOffice Web Comp（浏览器端 Office 文件处理）/ BOSS直聘爬虫（Chrome CDP）/ EdgeEver（Cloudflare 开源笔记）/ AI Shortlink（开源短链平台）/ FlareStarter（Cloudflare Workers SaaS 起步模板）/ Rheo（Typst 转 HTML/EPUB/PDF）；AI 相关=Claude 中国用户检测工具（一键识别是否被 CC 标记中国用户，另 ClaudeFlag）/ handmux（tmux 会话搬进手机浏览器操作 CC/Codex）/ WorldCupVoice（AI 实时体育赛事解说）/ Web Cursor（浏览器 AI 编码沙箱 WebContainer）/ 文译（大模型翻译 epub 默认 DeepSeek）；资源=Awesome Zhuiju Free（免费追剧）/ 开源中文存储技术书 / 山手线声音背景音。本期提及 GitHub repo 数条（microsoft/flint-chart、SakuraMathcraft/LaTeXSnipper、electroluxcode/onlyoffice-web-comp、eatmoreduck/boss-zhipin-scraper、tianma-if/edgeever、flyfish-dev/shortlink、FlareStarter/flarestarter、gokuscraper/claude-tester、handmux/handmux、zicojiao/worldcupvoice、siuming-qiu/web-cursor、BigDawnGhost/wenyi 等）——按 digest「GitHub 倉庫觀察」鐵律應集中列出、不散落本段。前一 baseline `402（2026-07-03 发布《我在智念 AI 的日子（小说）》）` — 2026-07-04 probe baseline（头条讽刺 AI 编程职场小说、特斯拉限制员工每周 AI 支出 200 美元、工具栏 Deno Desktop/EdgeMirror/Douzy）；前一 baseline `401（2026-06-20 前后发布《如何赚到10亿美元》）` — 2026-06-26 digest 处理（PG 牛津演讲 / Speedtest 被 12 亿收购 / SQLite 作者 OSS 维护负担 / HTTP QUERY / PACT 协议）；前一 baseline `400（2026-06-12 发布《rsync 的争论》）`；前一 baseline `399（2026-06-05 发布《中国 AI 大厂访问记》）`。

### Entry 範本

```
### <title — 簡短 probe 名>

- **Why**: <為什麼要 probe — context，之後 agent 整理結果時引用>
- **Source(s)**: <去哪查 — URL / repo / API endpoint>
- **How to fetch**: <agent 用什麼工具：`gh release view <owner>/<repo>` / WebFetch / `curl` JSON / Context7 / claude-in-chrome MCP>
- **Hit signal**: <什麼算「有新東西」— version > X / 上次 fetch 時間 / 特定 keyword 出現>
- **Action on hit**: <找到後做什麼 — inject 進當天 digest「外部訊號」段、標 priority、想抓細節 follow up>
- **Triggered by**: <`daily` / `stage-2-digest` / `both`> — `daily` 走 06:45 launchd probes wrapper；`stage-2-digest` 只在 digest pre-flight 由 agent 主動 fetch（適合 release 類低頻 / 高 cost / 需 user attention 的訊號、避免 daily spam）；`both` 兩邊都跑
- **Last seen**（optional）: <上次 baseline 值 — agent 每次 fetch 完更新這欄；空白代表第一次跑>
```

## Resolved / Archived

_(probe 不再 relevant 時移到這、保留歷史、一年後刪)_

## 維護規則

- **Hit signal 必填**：沒寫判準 agent 不知道 baseline 在哪、會每天 report 同樣內容；最簡單寫「上次 fetch 時間 / version / hash」
- **How to fetch 寫具體命令或工具**：不要寫泛泛「查 GitHub」，寫 `gh release view <owner>/<repo> --json tagName,publishedAt`，agent 才能照跑
- **Triggered by 必填**：`daily` / `stage-2-digest` / `both` 三選一。`daily` 走 06:45 launchd 自動跑；`stage-2-digest` 只在使用者叫 stage-2 digest 時 agent 主動 fetch；`both` 兩邊都跑。**daily 跑時 agent 過濾掉 `stage-2-digest` only 的 entries**（讀 PROBES.md 時跳過、不 fetch 不 surface）；**digest 啟動時 agent 讀 PROBES.md 過濾 `stage-2-digest` + `both` entries 跑**（不論 daily 是否已跑過、digest 啟動時都跑一次拿即時 diff）。
- **Last seen 由 agent 更新**：跑完 probe 後若 hit signal 達標，agent 把新 baseline 寫回對應 entry 的 `Last seen` 欄（直接 Edit 本檔，commit 由使用者決定）
- **空 PROBES.md 時 wrapper 跳過**：probes-daily.sh 偵測無 Active entry → 產極簡 report 「今日無 active probe」直接結束

## Output

寫到 `reports/local-analysis/{date}-probes.md`（gitignored，跟其他 4 channel local-analysis 同 dir）。
