# Probes — 主動拉外部訊號

> 手動 maintain 的「需要主動去查、不能等被動社群討論浮上來」的源。Agent 跑時讀此檔，對每個 active probe 跑對應工具 fetch、結果寫進 `reports/local-analysis/{date}-probes.md`（daily channel）或 surface 進 `reports/digest-{date}.html`（stage-2-digest channel）。
>
> 主動 = 直接拉外部 source（GitHub API / RSS / web）。被動比對既有 raw md 的東西放 `WATCH.md`。
>
> **觸發**（按 entry 的 `Triggered by` 欄位分流）：
> - **`daily`**：launchd `com.gggodlin.local-analysis-probes` 每日 06:45（在 4 channel 之後）→ 結果寫進 `reports/local-analysis/{date}-probes.md`
> - **`stage-2-digest`**：每次使用者叫 stage-2 digest 啟動時 agent 主動 fetch → surface 進 digest「🆕 ... 動態」section（不依賴 06:45 daily wrapper、避免低頻訊號 spam）
> - **`both`**：兩邊都跑
> - **手動**：使用者說「跑 probes」/「probes 一下」/「拉一下 probes」→ agent 讀 PROBES.md 對所有 entries 跑（不論 `Triggered by`）

## Active

### anthropics/claude-code release watcher

- **Why**: 使用者直接用 CC CLI（cmux + 多 worktree + ad-hoc session + launchd 周邊 routine）、上游每次 release 都可能影響 TUI 行為、hook 機制、daemon、MCP timeout、quota、deprecation。要在「每次叫 stage-2 digest 的當下」即時 fetch、從上次看的版本 diff 到最新、寫進 digest「🟣 Anthropic / Claude 動態」的「🆕 CC CLI release」小節（2026-06-20 起併區、原獨立段「🆕 CC CLI 動態」收為本區第一小節）。
- **Source(s)**: <https://github.com/anthropics/claude-code/releases>（via `gh release list anthropics/claude-code`）
- **How to fetch**: `gh release list --repo anthropics/claude-code --limit 10 --json tagName,publishedAt,name`，filter `publishedAt > Last seen`；對每個新版跑 `gh release view <tag> --repo anthropics/claude-code --json tagName,publishedAt,body` 拿 changelog 前 2000c。
- **Hit signal**: 最新 release `tagName` 比 `Last seen` 新 → 有新 release。
- **Action on hit**: stage-2 digest 寫進「🟣 Anthropic / Claude 動態」的「🆕 CC CLI release」小節、列每個新 release 的 tagName + publishedAt + body 重點 entry（按 hook / daemon / MCP / quota / deprecation / fast mode / agents 等分類）+ 每條標跟使用者 workflow 對應的個人化評論 + 顆星排序（★/★★/★★★）+ 升級指令。
- **Triggered by**: `stage-2-digest` only（**不在 06:45 daily probes 跑** — release 低頻、daily 跑會 spam；digest 階段才有差分價值）
- **⚠️ 勘誤（2026-07-25，讀下方歷史 baseline 前先看）**：舊 baseline 文字多處寫「local-analysis 19-channel」，**實際為 22 個 channel**（`CHANNELS` 陣列現查為準：LLM 13 + shell 9、daily 14 / weekly-tue 8）。另舊 baseline 把 advisory 性質的 guideline 寫成「上限 / 會被限制」屬推論錯誤（例：「19 個 channel 已超過新預設的 15 上限」「貼近 20 的預設上限」）——workflow 內 `agent()` 併發本就由 runtime 排隊（cap `min(16, cpu-2)`），channel 數多於該 cap 只是排隊、不觸發攔截或警示；2026-07-25 digest 實測單次跑滿 161 agents 零攔截。歷史文字保留原樣不重寫，以本行為準。
- **Last seen**: `v2.1.227 (2026-08-10T22:56:53Z)` — 2026-08-11 probe baseline（距前版 v2.1.226 約 2.8 天、僅 1 個新 release；重點：修復以過期登入 token 開啟的 session 在未帶入使用者訂閱 tier 情況下評估 feature flag，可能誤讓 Max plan 使用者被提示為 Fable 啟用付費 usage credits（★★，命中 quota/billing 紀律與 Fable per-model 額度追蹤，避免誤付費的落地修復）；修復 `claude-code-action` 在 GitHub-hosted runner 搭配 `allowed_non_write_users` 時所有 Bash 指令失敗（★，未見使用者走 claude-code-action GitHub Actions 路徑）；修復 `/tui` 會把已 rewind 到第一則訊息之前的對話重新帶回（★，session/tui 使用習慣、低頻場景）；改進 slash-command 選單：藍色只標示選中列、匹配字元改用粗體而非變色、emoji/重音字元名稱保留原字形（★，UX 細節）；改進效能：減少 file-not-found suggestion 與 at-mention size check 的 event-loop 停頓（★★，命中一般效能/可靠性紀律，長 session 互動延遲降低）。以上逐項對帳 `gh release view v2.1.227` 的 5 條 bullet、無漏項。升級指令：CC 具備自動更新，本機 `claude --version` 2026-08-11 現查已是 `2.1.227 (Claude Code)`（無需手動操作）；如需強制檢查可跑 `claude update`；前一 baseline `v2.1.226 (2026-08-08T02:48:05Z)` — 2026-08-09 probe baseline（涵蓋 v2.1.225 + v2.1.226 兩版；v2.1.225 重點：gateway spend-limit 支援 usage warning（★★，命中 cc-vendor-bridge gateway + quota 追蹤紀律）；`claude agents` 對未信任目錄新增 workspace trust prompt（★★，命中 agent 安全紀律）；修復短暫 401 用短效 token 覆蓋長效 `CLAUDE_CODE_OAUTH_TOKEN` 導致 headless session 中斷（★★，命中 headless/launchd 可靠性）；修復 macOS MCP OAuth server 因 keychain timeout 間歇整批 401（★★，命中 macOS 環境診斷）；修復 auto mode 誤把 safety-filter 拒絕計入連續封鎖上限（★★，命中 auto mode 安全紀律）；修復跨 session 訊息在 headless/startup 卡住無通知（★★，命中多 agent 協作）；SendMessage 現可用名字主動對其他機器 Remote Control session 發起對話、`ListAgents` 顯示 `name [ref]`（★★★，直接命中本次任務用到的 SendMessage/ListAgents deferred tool）；SendMessage 對已確認收件者不再被同名本機 session 誤換（★★，同一可靠性脈絡）；其餘較不相關項目（Remote Control resume 歷史損壞修復、hover 誤改啟動目錄、self-hosted-runner startup 報錯、web session 誤報卡住、Remote Control 附圖改進、[VSCode] Focus view 折疊修復）均標 ★、未見使用者走相關路徑。v2.1.226 僅「Bug fixes and reliability improvements」一行、無細節。以上逐項對帳 `gh release view v2.1.225`/`v2.1.226`，v2.1.225 14 條 bullet 無漏項、v2.1.226 無 bullet 可拆。升級指令：CC 具備自動更新，本機 `claude --version` 2026-08-09 現查已是 `2.1.226 (Claude Code)`（無需手動操作）；如需強制檢查可跑 `claude update`；前一 baseline `v2.1.224 (2026-08-07T04:00:59Z)` — 2026-08-07 probe baseline（距前版 v2.1.223 約 1 天、僅 1 個新 release；重點：移除 200-subagent-per-session 生成上限，長跑 session 不再拒絕新 agent（concurrency/depth 限制仍在）（★★★，命中 dispatch/subagent-routing 紀律；⚠️ 2026-08-08 第 1 輪 audit 更正：移除的是**單一 session 累計 spawn 上限**，官方原文明寫 concurrency 與 depth 限制未變。本 repo 已知單次最高 161 agents（2026-07-25，語境是 workflowSizeGuideline advisory），161 < 200、從未觸及此上限，故對現行 fan-out 規模無實際影響，只是拉高未來長跑 session 的天花板）；跨機器 cross-session `SendMessage` 正式支援 macOS 與 Linux、並新增 `ListAgents` 探索其他 session（★★★，命中多 session/多 agent 協作與 handoff 場景，是本次任務本身可用的 deferred tool 之一）；Bash tool 描述變更為明文提示「指令輸出顯示給模型看、不保證使用者看得到」（★★★，直接佐證使用者長期要求的「答案必須進最終聊天訊息」紀律——CC 官方現在把這點寫進工具描述本身）；新增 `archive` plugin source，可用 zip 檔透過 HTTPS 免 git/npm 安裝 plugin、可選 SHA-256 pinning（★★，命中 safe-trial 撰寫紀律 + npm-supply-chain-defense 研究，多一種安裝管道也多一種要稽核的來源）；新增 `crossSessionInbound` / `dialogExpiry` 設定——bypassPermissions session 收到的跨 session 訊息現在會被 hold 待批准、其餘 session 訊息維持自動送達（★★，命中 auto mode 安全紀律與「不可逆/破壞性動作」授權邊界）；新增 sandbox credential-masking 選項（`extract`/`onExtractNoMatch` 結構化 env 值、`decode: "jwt"` + `maskClaims`、AWS SigV4 re-signing，需搭 `network.tlsTerminate` 且僅 user/managed/`--settings` 生效）（★★，命中 mutation-tool-safety-design 的 credential 保護紀律，是官方對「別讓 agent 碰真密碼」的落地實作）；修復超過 200 字元的 project path 會 resolve 到另一個 project 的 session 目錄、導致 session list/rename/fork/delete/`/resume` 跨 project 污染（★★，命中 session 管理正確性；本 repo 有 `~/code/social-info` / `~/Desktop/projects/social-info` 雙路徑但長度遠低於 200 字元、低機率觸發）；修復 `SendMessage` 對投遞失敗仍回報「Message sent」、改為如實回報錯誤（★★，命中多 agent 協作可靠性）；修復 sandbox filesystem deny 規則若寫成 trailing slash（如 `denyRead: "~/.aws/"`）在 Linux/macOS 會被靜默繞過（★★，命中 gate-authoring / sandbox 安全紀律）；修復 sandbox violation 細節從未出現在 Bash tool 結果、現在 Claude 能看到被拒的具體檔案/網路存取與原因（★★，命中 sandbox debug 場景）；修復 mid-turn 連上的 MCP tool 被 tool search 延後載入卻沒把工具名告知 model 的問題（★★，命中本次任務正在用的 deferred-tool/ToolSearch 機制本身）；修復同一 plugin 裝在多個 project 時 install 記錄會被靜默 corrupt（★★，命中 skill/plugin 生態使用習慣）；改進 fullscreen mode，scrollback 保留完整 pre-compaction 歷史（不只最近一段區間）（★★，命中長 session / compaction 使用習慣）；feedback survey 的 transcript share 現在（經同意後）也會上傳最後一次請求的 model settings——含 system prompt（即 `CLAUDE.md` 內容）、tool definitions、model parameters，密鑰仍會被 redact、過大時優先丟棄這些欄位（★★，命中 CLAUDE.md 內容外流風險，雖是 opt-in 但值得留意分享 transcript 時的實際上傳範圍）；其餘較不相關項目：self-hosted-runner Team/Enterprise 專屬功能（★，個人帳號不適用）、移除不可用貼上文字時新增 cancel-and-confirm 步驟（★，UX 細節）、Bedrock `ANTHROPIC_BEDROCK_REGION_PREFIX` env var（★，未見使用者走 Bedrock）、recalled/restored paste 內容偶發抓錯資料或漏字的修復（★，一般穩定性）、Wayland copy-on-select race 修復（★，Linux 專屬、使用者是 macOS）、feedback survey transcript share 在長 session 靜默失敗改為報錯（★，一般穩定性）、Remote Control auto-start 在 cold start + stale login token 下偶發失敗的修復（★，未見使用者用 Remote Control）、Remote Control/SDK client 在 `/clear` 等無輸出指令後顯示空白訊息的修復（★，同上）、Remote Control session 在 server session 過期後重建時誤上傳舊對話歷史的修復（★，同上）、Remote Control 改進讓 attached web/mobile client 看得到 compaction 進度（★，同上）、Remote Control 連線失敗改為持續顯示失敗指示 + 重連捷徑（★，同上）、managed settings 的 approval prompt 在 re-login/org 切換後不再重跳（★，org 場景不適用）、recalled paste placeholder 編號重新排序（★，UX 細節）、Remote Control 改為 archive 舊 server session 而非留死連結（★，未見使用者用）、[VSCode] extension 顯示 Remote Control 已連線但實際連線失敗的修復（★，未見使用者用 VSCode）、修復 session resume 時會在使用者已關閉 Remote Control 後又靜默重連（★，未見使用者用）、[VSCode] session 未遵守 `remoteControlAtStartup` 設定的修復（★，同上）。以上逐項對帳 `gh release view v2.1.224` 的 31 條 bullet、無漏項。升級指令：CC 具備自動更新，本機 `claude --version` 2026-08-08 收尾現查為 `2.1.225 (Claude Code)`（無需手動操作）；如需強制檢查可跑 `claude update`。⚠️ **2026-08-08 收尾勘誤**：`v2.1.225`（2026-08-08T01:09:26Z）在本次 probe 執行當下其實已發布卻未被列入（同日 digest 誤寫成「僅 1 個新 release」，已更正），baseline 刻意留在 v2.1.224 不推進，好讓下次 probe 一定會把 v2.1.225 帶回對帳；前一 baseline `v2.1.223 (2026-08-06T00:52:37Z)` — 2026-08-06 probe baseline（距前版 v2.1.222 約 26 小時、僅 1 個新 release；重點：Bash permission bypass 修復——精心構造的指令可讓部分內容躲過 permission check（★★★，直接命中「不可逆/破壞性動作」安全紀律 + Bash 指令審核習慣）；permission prompt 對 tab / 不可見 Unicode 填充的指令同樣補洞，approval dialog 不再被藏字（★★★，同一安全脈絡的配套修復）；workflow script 用動態 `import()` 逃出 workflow sandbox 的漏洞修復（★★，命中 workflow-hardening skill 撰寫紀律，dynamic workflow script 場景直接相關）；`/review` 改為 `/code-review` 的 alias（審當前 diff 或 PR，`/code-review <level> <pr#>`；`/code-review ultra` 觸發 deep cloud review）（★★★，命中日常 code review 使用習慣，指令面已合流）；`/code-review` 未帶 effort level 時沿用上次輸入的 level（★★，命中 effort 控制紀律，減少重複打字）；`CLAUDE_CODE_DISABLE_1M_CONTEXT` 語意變更——改為把所有具原生 1M window 的 Claude 模型透過 auto-compaction 統一鎖回 200K（不只原本固定清單），auto-compaction 沒能鎖住 200K 時 startup 會跳警告（★★，命中主 session 的 `opus[1m]` 設定——`~/.claude/settings.json` 的 `model` 欄即 `opus[1m]`——與 context budget 控制；本機未實測此 env var 對 `opus[1m]` 的實際行為）；auto-compact 對「未知 model ID」的 session 改為主動鎖在假定的 context window 內（原本會讓 context 無限長超過真實上限），要恢復舊行為需設 `CLAUDE_CODE_DISABLE_UNKNOWN_MODEL_WINDOW_ENFORCEMENT=1`（★★，直接命中 cc-vendor-bridge 第三方 endpoint 派工——ccp-glm/ccp-gpt/ccp-bruce 這類 vendor bridge 常帶非標準 model ID，此變更影響其 context 管理行為）；workflow agent / forked skill / slash command / resumed background agent 請求的 subagent model 被 org 政策限制、改回 parent model 執行時現在會跳警告（原本靜默 fallback）（★★，命中 subagent-routing 模型選擇機制 + Fable 三入口失敗降級紀律，警告讓 silent downgrade 變 visible）；`modelOverrides` 裡非 Anthropic model ID 的 key 原本會被誤當成 session 的 canonical model ID，修復後未知 key 依文件行為忽略（★★，命中 cc-vendor-bridge / model swap 設定正確性）；forked background agent 在 resume 時若重建 fork 的 parent prompt 失敗，會卡在「already resuming」整個 session 動不了，此問題已修（★★，命中背景 agent 監控紀律）；resumed session 因歷史內含 malformed diagnostics attachment 導致每輪都失敗、或卡在無回應錯誤畫面，已修（★★，命中 session resume 穩定性）；解析異常 `git push` 輸出時偶發 hang 已修（★★，命中 daily run-daily.sh 的自動 git push 工作流，理論上降低 06:00 launchd job 卡死風險，但與已知 pipeline hang bug 屬不同成因、不可直接當作根治）；`/cd` 中途切換後 resume session 若回空值的問題已修（★，命中 session 使用習慣，未見使用者頻繁用 `/cd`）；gateway model discovery 原本會漏掉用 provider-prefixed ID（如 `vertex_ai/claude-*`、`bedrock/anthropic.claude-*`）註冊的 Claude 模型，已修（★，vertex/bedrock 場景與使用者當前 vendor bridge 選型不同，低相關）；`bypassPermissions` 模式的 agent 定義原本會忽略 org 層級的 bypass-permissions disable 政策，已修（★，org 政策場景、個人帳號不適用，但命中 agent-authoring 的 tools/permission 欄位撰寫紀律）；managed settings 的 server-delivered 設定原本會蓋掉機器本地 `managed-settings.json` 或 MDM profile 的 env block，改為逐 key merge（★，企業 MDM 場景、未見使用者走此路徑）；沙盒指令在 Linux 上因 `sandbox.filesystem.denyWrite` 涵蓋工作目錄而啟動失敗的問題已修（★，Linux 專屬、使用者是 macOS）；新增 `"owner/*"` wildcard 到 `strictKnownMarketplaces` / `blockedMarketplaces` managed settings，可整個 org 的 marketplace repo 一次允許或封鎖（★，org marketplace 治理場景、個人帳號不適用）；cloud session 新增 `/teleport` hint，提示用 `claude --teleport <session id>` 接回本機（★，未見使用者用 cloud session）。以上逐項對帳 `gh release view v2.1.223` 的 19 條 bullet、無漏項。升級指令：CC 具備自動更新，如需強制檢查可跑 `claude update`（2026-08-06 未實測本機 `claude --version`，只查 GitHub release API）；前一 baseline `v2.1.222 (2026-08-04T22:39:55Z)` — 2026-08-05 probe baseline（其餘歷史鏈維持不變，詳見前次 baseline 全文）

### ruanyf/weekly 新一期 watcher

- **Why**: 阮一峰《科技愛好者周刊》每週五出一期，內容近兩年高度集中在 AI 編程 / 模型經濟 / 軟體開發方法 / 工程師職涯，跟使用者 daily-topic-radar、interview-tour-2026、本機 AI 工具研究的選題重疊度極高（第 398 期頭條就是 OpenClaw 創辦人曬 Token 用量、Uber 燒爆 AI 預算、微軟棄用 Claude Code）。當成固定外部視角源，有新一期就在當天 digest 摘要+個人化評論。
- **Source(s)**: <https://github.com/ruanyf/weekly>（README.md 是全期索引、`docs/issue-N.md` 是各期正文）
- **How to fetch**: 用 authenticated `gh`（匿名 `curl` 打 GitHub API 會撞 rate limit 403）。取最新期號：`gh api repos/ruanyf/weekly/contents/README.md --jq '.content' | base64 -d | grep -oE 'docs/issue-[0-9]+\.md' | head -1 | grep -oE '[0-9]+'`。若 > Last seen，抓正文：`gh api repos/ruanyf/weekly/contents/docs/issue-<N>.md --jq '.content' | base64 -d`。
- **Hit signal**: 最新期號 > `Last seen` 的期號 → 有新一期。
- **Action on hit**: stage-2 digest 開「📰 科技愛好者週刊新一期」section、摘該期 12 個固定欄目（封面圖 / 本週話題社論 / 科技動態 / 文章 / 工具 / AI 相關 / 資源 / 圖片 / 文摘 / 言論 / 往年回顧）重點，標跟使用者 stack / 求職 / AI 工具研究對應的個人化評論。**正文裡的 GitHub repo 按 digest「🛠 GitHub 倉庫觀察」鐵律集中、不散落本段**。
- **Triggered by**: `stage-2-digest` only（**不在 06:45 daily probes 跑** — 週刊一週才一期、比 CC release 更低頻，daily 比對無差分價值徒增 gh call；digest 階段比對 Last seen 即可 catch）
- **Last seen**: `407（2026-08-07 發布《國家為什麼需要開源軟體？》）` — 2026-08-07 probe baseline，下次 trigger = 第 408 期。內容=頭條社論《國家為什麼需要開源軟體？》— 大模型 Kimi K3 開源引發美國政府考慮限制中國開源模型，隨後微軟、輝達、思科、Cloudflare 等美國科技公司聯署公開信《開放權重模型和美國 AI 領導力》，核心論點：AI 領導力取決於能否建立開放且滲透各領域的生態，限制開源模型最終只會讓 AI 落入少數大公司手中——評：命中 llm-model-landscape 研究脈絡（開源模型地緣政治張力）+ ai-coding-era-engineer-moat 研究（AI 集中在少數大廠手中的護城河討論）。次篇《你可以專注多久？》— 加州大學爾灣分校 20 年調查：人保持專注的時間從 2004 年 150 秒降到 2025 年僅 47 秒，且分心後平均需 25 分 26 秒才能恢復專注狀態——評：呼應使用者 mission-critical infra 工作風格（保持流程精簡、避免不必要 context switch）。科技動態=三面咬合拉鏈 1985 年專利今年靠 3D 列印重新被 MIT 研究者利用（可同時提供剛性與撓性、取代金屬支撐桿）/ 美團與蘇州公安「紅燈停表」功能替外送員扣除紅燈等待時間 / 英國設計師用廢棄網球做鞋（實驗性、量產存疑）/ 歐洲團隊研究發現鳥類「起飛距離」對女性比對男性更遠（假說：演化上男性獵大型獵物、女性獵鳥類等小型獵物）。文章=Chrome 瀏覽器終於支援 ARM64 Linux 官方下載 /《深入剖析 AI Token 中繼市場》剖析國內比原廠更便宜的大模型 API 路由轉售服務內幕——評：直接命中 cc-vendor-bridge 生態（ccp-glm/ccp-gpt/ccp-bruce 這類第三方 endpoint 派工）與 CN model swap landscape 研究，可留意是否有新可評估的 relay 服務 / SwiftUI 七年後仍像測試版的批評文（蘋果工作文化從精益求精轉向「足夠好」）/ Coldcard 硬體錢包被靠公鑰算出私鑰破解、損失逾 1 億美元 / 坦克為何幾乎不用 Windows / 2026 年自架郵件伺服器實務筆記。工具=Gander（安卓離線檔案檢視器）/ StatLite（Spring Boot 自架儀表板）/ presenterm（終端機播放 Markdown 簡報的 CLI）/ Reconya（自架局網設備掃描 Web 介面）/ Quickdraw（可嵌入 React/RN/JS 的無限畫布白板 SDK）/ 魔尺（開源 3D 魔尺造型設計網頁工具）/ LiteSnap（開源 Windows 截圖工具）/ 水杉輸入法、rime.vim（開源 Windows/Vim 中文輸入相關工具）/ history4feed（自架的 Feed 全文自動抓取工具，只提供 API）——評：history4feed 的定位與 daily-topic-radar 自身 RSS/feed 抓取機制方向類似，可留意是否有可借鑑之處（對應本 repo「已知 fetcher gap」段的補全思路）。AI 相關=Codex Security（OpenAI 官方基於 Codex 的 CLI 代碼漏洞掃描工具）——評：與 security-review skill + npm-supply-chain-defense 研究方向相關，可留意跟 bumblebee daily scan 的互補或取代路徑 / Palmier Pro（開源提示詞驅動 AI 視頻編輯器，低相關）。資源=歐元紙鈔設計競賽入圍作品站 / 洛杉磯像素地圖（標科技公司位置）/ 世界拉麵館照片收集站 /《宋飛正傳》可視化網站。圖片=巴黎公共設施大量嵌入綠化元素（候車亭、路燈、書報亭爬滿植物，但常因環境不適合植物生長而枯萎或雜草化）。文摘《編程的三個時代》— 演算法時代（1940s 末–1970s，高階語言與編譯器）→ 物件導向時代（1970s–2000s 初，現代程式語言）→ 系統化時代（2000s 初至今，函式庫/平台/API 組合）；AI 編程沒有開啟新時代，只是讓第三個時代加速——評：直接命中 ai-coding-era-engineer-moat 研究核心論點（AI coding 是既有典範加速、非典範轉移）。言論=費曼 1974 年加州理工畢業演講「你的首要原則是不要騙自己，因為你是最容易被自己騙的人」/ HN 讀者預測 2026 是人類獨攬費爾茲獎最後一年、之後得獎者都將與 AI 合作撰寫論文 / 家庭實驗室改造心得讚 Ubiquiti 路由器 UX 遠勝華碩 /《華爾街日報》「對普通任務用最強最貴 AI 模型，就像開蘭博基尼去買牛奶」——評：直接命中 subagent-routing 模型分級紀律（機械任務不該無腦上最貴模型）與 effort 控制紀律，是現成的類比佐證 /《80 年代的程式設計體驗》— AI 省下無意義的手動查閱與輸入時間，但也讓人失去「必須逐行讀懂、下苦功鑽研」的投入——評：呼應 ai-coding-era-engineer-moat 研究對 AI 影響工程師技能養成的討論。本期提及 GitHub repo 多條（mokshablr/gander、PVRLabs/statlite、mfontanini/presenterm、Dyneteq/reconya、quickdrawjs/quickdraw、regomne/magic-snake、HuibingLin/LiteSnap、metasequoiaime/MetasequoiaImeTsf、TSalmon3/rime.vim、muchdogesec/history4feed、openai/codex-security、palmier-io/palmier-pro 等）——按 digest「GitHub 倉庫觀察」鐵律應集中列出、不散落本段。前一 baseline `406（2026-07-31 发布《道可，道非，常道》）` — 2026-07-31 probe baseline，下次 trigger = 第 407 期。内容=头条社论《道可，道非，常道》— 华为「心声社区」转发对半导体业务总裁何庭波的专访，附任正非序言：τ 定律是华为在美国制裁下唯一能选的突围路，序言标题借老子《道德经》「道可道非常道」的另一种断句「道可，道非，常道」（走对的路、走错的路，才是永恒的路）——评：呼应使用者 mission-critical infra 工作风格（在结构性限制下摸索出固化流程、不纠结路径本身对错）。本周软件=OpenConnector（开源密码连接网关，与 AI Agent 配套：统一保管密码/授权凭证，Agent 只拿账号标签+元数据+执行结果、不碰真密码，支持 1000+ 身份提供商/10000+ 应用，可部署 Cloudflare Workers/Fly.io+SQLite/本地 Node.js/Docker，也有 SaaS 云服务）——评：直接命中使用者 feedback_mutation_tool_safety_design（mutation tool 安全设计纪律：token binding / 不让 agent 碰真凭证），是该纪律的现成实现范例。科技动态=长三乙火箭升空 47 秒遭雷击仍安全入轨（法拉第笼防雷设计+高温尾焰接地）/ 特斯拉推出 225 美元童车（无踏板、宣称自平衡）/ 意大利艺术家青铜手握树 14 年实验（树干绕手继续生长）/ 美国教育部新规：毕业收入低于同岗未受高等教育者的专业将取消助学贷款资格。文章=GPT-5.6 Sol/Claude Fable 5/Grok 4.5/Gemini 3.6 Flash 网页手绘蒙娜丽莎对比（GPT-5.6 Sol 领先、Grok 4.5 几乎不可用，Claude Fable 5 质量第二但成本与耗时垫底、近其他模型 20 倍）——评：直接命中 claude-api skill 触发规则（Claude/model 相关必先查）+ model landscape 追踪，Fable 5 高成本高耗时低性价比的側寫可与既有 Fable 三入口机制观察对照/BitTorrent 发明者 Bram Cohen 故事/Emoji 起源于日本 90 年代高额移动网络资费+日语敬语的省字需求/3D 打印废料马赛克/Linux 自建证书颁发机构教程。工具=Kagi 出品隐私浏览器 Orion（WebKit 内核、同时支持 Safari/Chrome/Firefox 扩展、零遥测承诺）/ 声波传数据开源工具 Chirp/ 单 HTML 文件 PPT 编辑器 Bento Slides/ rsync 终端 UI lazyrsync/ macOS 启动项统一管理工具 Birth/ Debian/Armbian 变身路由器 Linux Router/ GitHub 仓库本地镜像同步 RepoMirror/ 内置 MCP 服务器的多协议下载器 FluxDown/ 基于 Telegram Channel 的小文件存储米饭云盘/ 网页密码保护 JS 库 StaticShield。AI 相关=Windows AI 磁盘清理工具 ai-disk-cleaner/ 需自购硬件+机械臂+摄像头操作 iPhone 的 AI 系统 PhysiClaw/ 让 Claude Code、Cursor、Codex 直接读写 Figma 设计稿的 MCP 服务器 Figwright——评：命中 figma-context-budget skill + Figma MCP 使用场景，可留意是否有互补/替代路径。资源=儿童分步绘图教程 How to Draw/ 网页滚动动画效果收藏站 scroll-driven-animations/ 在线围棋教程 online-go。图片=巴西宝特瓶折射灯发明（15 国流行的免电室内采光方案）。文摘《世界最不知名的最强程序员》— 法布里斯·贝拉尔（Fabrice Bellard）独自编写 FFmpeg / QEMU / TCC / QuickJS 等支撑 YouTube、Netflix、AWS/GCP/Azure 虚拟化底层的基础设施代码，从不做市场推广、无社交账号，近年转向本地 LLM 相关工具（TextSynth Server 本地跑 LLM、ts_zip/ts_sms 用大模型压缩文本、NNCP 神经网络无损压缩）——评：与使用者 apple-silicon-local-llm-serving（本地 LLM serving 研究）+ ai-coding-era-engineer-moat（工程师护城河研究）高度呼应，「深耕单一基础设施领域、不追热点、不做市场推广」是这两个研究主题里少见的极端范例，值得作为对照案例记住。言论=HN 读者吐槽登录/注册按钮大小暗示网站偏好新用户而非老用户/《无人阅读的代码》「程序员职责是在模糊与限制中降低代码风险，不写代码的 AI 时代更甚」/ Simon Willison 谈退休老将因 AI 带来的新机遇重出江湖/ Eric Meyer 反驳「CSS 是臃肿大杂烩」论（是野心而非臃肿）。本期提及 GitHub repo 多条（oomol-lab/open-connector、solst-ice/chirp、westpoint-io/lazyrsync、iAmCorey/birth、Jaksay/Linux-Router、wutongyuonce/RepoMirror、zerx-lab/FluxDown、AndySkaura/mifun-storage、wangshengithub/staticshield、vudsen/ai-disk-cleaner、physiclaw/PhysiClaw、awdr74100/figwright 等)——按 digest「GitHub 倉庫觀察」鐵律應集中列出、不散落本段。前一 baseline `405（2026-07-24 发布《资源，社会公平与算力》）` — 2026-07-24 probe baseline，下次 trigger = 第 406 期。内容=头条社论《资源，社会公平与算力》— 引奥地利哲学家伊里奇 1973《能源与公平》「资源过剩加剧社会不公平」论：人均用电量低于阈值时增加用电量利于公平，超过阈值后能源增长以牺牲公平为代价；车速同理，速度提升让少数人领先、多数人被拉开距离；套用到算力——普及初期提高全民福利，但算力不再稀缺后只会让少数人拥有更大算力优势、加剧不公平，作者建议政府应设算力上限——呼应使用者 apple-silicon-local-llm-serving（本地消费级硬件算力受限，被排除在「算力过剩少数人」之外）+ AI career landscape 研究（算力/资源集中在头部玩家手中的趋势）。次篇《如何破坏一个组织》— 1944 年美国战略情报局（OSS）解密手册《简单破坏实战手册》，核心破坏法=让组织官僚化（一切提交委员会决策、纠结措辞、动辄开会、严格走流程、文件冗长、增加审批环节），六十年后仍适用于任何组织——反向呼应使用者 mission-critical infra 工作风格（保持流程精简、避免过度工程化正是这份手册破坏法的对立面）。科技动态=Akamai 印满混淆 Bash 脚本的汗衫（OCR+Base64 解码后是正弦波文字画）/ 西北大学隐身无人机（高速旋转镂空机身骗过摄像头、噪音仍会暴露）/ 高德地图「位置口令」6 位数字定位分享（15 分钟-24 小时有效期，非固定码）/ 美国批准太空镜卫星计划（18x18米镜面反射阳光照明，每小时 5000 美元，最终目标 5 万面镜子）。文章=姚期智上海世界人工智能大会演讲《AI 的第一性原理：人工智能的威力及其局限》（中文，谈 AI 能力边界，呼应 AI career landscape / moat 研究）/ 美国政府没收比特币冷钱包私钥机制揭秘 / 认证令牌应放 Cookie 而非 localStorage（前端安全）/ 跨服务器快速复制 SQLite 数据库方法 / git exclude 用法（本地忽略文件不写进 .gitignore、不影响他人，比 gitignore 更适合个人化本地排除）/ CSS 网页打印样式入门。工具=Satteri（Rust 写的 JS Markdown 引擎、插件化转 HTML）/ AsteroidOS（智能手表开源系统、基于 Linux 非安卓）/ GNU TeXmacs（跨平台科技文档编辑器）/ Elysia（TypeScript 轻量 Web 框架）/ CloudFlare-ImgBed（零成本图床）/ DetectRadar（核对出口 IP 与浏览器时区/语言/WebRTC/DNS/IPv6 泄露一致性，在线试用）/ Véspero（代理网关）/ Foursmith VR（手机+平面显示器 VR 播放器）/ Codia（代码片段美化）/ Email Builder（React 拖拽邮件模板编辑器）。AI 相关=Timeline Studio（浏览器本地跑视频 AI 编辑器，素材不上传、含自动字幕/人声分离/数字人）/ Gaia（基于字节 flowgram.ai 的工作流编辑器）/ 腾讯 AI-Infra-Guard（朱雀实验室 AI 安全测试平台，扫描 AI 应用漏洞）/ SurfSense（NotebookLM 开源替代品——直接命中使用者 user_video_research_notebooklm_default + video-research-toolchain wiki，NotebookLM 重度使用者可留意的自架替代选项）。资源=WindowSwap（世界各地窗外实景随机浏览）/ 从零构建 Redis 动手课程 / CSS Flexbox Playground。图片=洛杉矶中央图书馆百年庆世界最大立体书 / 联系页设计吐槽（表单 vs 「快滚」画风对比）。文摘《宗族对文明不利》— 宗族内部合作高于对外合作、抑制个人奋斗（成功被宗族分享）。言论=一加公司「极客手机」初心 vs 现实妥协退出欧美市场对比 / HN 读者「AI 真正风险是让懒惰看起来像高效」（呼应使用者 mission-critical infra 工作纪律——警惕 AI 使用的效率感与实际产出落差）/《一个 SRE 工程师的 AI 观点》批评新词造权威 / 《做一个软件工程师的疯狂》建筑业分工 vs 全栈程序员一人包办（直接呼应使用者 ai-coding-era-engineer-moat / taiwan-ai-eng-job-market 研究——AI 时代全栈单人开发模式可持续性讨论）。本期提及 GitHub repo 多条（bruits/satteri、MarSeventh/CloudFlare-ImgBed、harrisonwang/detect-radar、tianrking/Vespero、foursmith/vr、AaronConlon/codia、heiheihoho1213/monto-email-builder、MartinDelophy/ai-video-editor、boommanpro/gaia-workflow-engine、Tencent/AI-Infra-Guard、MODSetter/SurfSense 等）——按 digest「GitHub 倉庫觀察」鐵律應集中列出、不散落本段。前一 baseline `404（2026-07-17 发布《你需要知道的 AI 内存知识》）` — 2026-07-17 probe baseline，下次 trigger = 第 405 期。内容=头条《你需要知道的 AI 内存知识》— 本地跑 AI 模型选硬件的两难：独立显卡（RTX 5090，32GB 显存/104.8TFLOPS FP32/1792GB/s 带宽）算力强但显存太小，70B 参数模型 4bit 量化需 32.6GB 已超显存上限跑不动；板载统一内存 mini PC（AMD Strix Halo Ryzen AI Max+ 395，128GB 内存/14.8TFLOPS/256GB 带宽）容量够但带宽仅显卡 1/7，40GB 模型每秒理论只能读 6 次即 6 token/s、龟速；MoE 架构（Qwen3-30B-A3B 仅激活 3B 参数）让统一内存机型每秒可生成 100+ token 变得可用；另一缺点是板载算力慢导致长提示词处理动辄几十秒到几分钟；结论：现阶段本地跑大模型无论独显或统一内存都有缺陷，只能跑中等规模 MoE 模型且提示词不能过长——与使用者 apple-silicon-local-llm-serving wiki（M5 Pro 48GB 本地 LLM agentic serving、dual-ceiling 物理 RAM+cognitive protocol、本地 27-35B 串 CC daily-driver 死路结论）高度呼应，可交叉引用验证。次条《布尔变量如何起名》— 四前缀 is-/has-/can-/should- 起名法 + 永远不用否定词命名布尔变量。科技动态=OpenAI 键盘（Jony Ive 设计、230美元快捷键批准/否决 AI 代理操作、RGB 灯显示代理状态，评价毫无新意）/ 天问二号抵达小行星 2016HO3 传回照片（20米不规则形状碎片、下一步采样返回）/ 荷兰设计师风力编织机（阳台风力驱动毛线编织围巾）。文章=机器学习博士求职指南（DeepMind 入职回顾）/ 使用 DNS 通过 ACME 认证（HTTPS 证书域名验证）/ Go 语言的困境（论前途因慢语言变快、难语言变易而被中间地带挤压）/ JavaScript 哈希算法速度比较（MD5 vs SHA256）/ 终端控制字符一览表（33 个命令行控制符快捷键图）/ 理查德·费曼加入创业公司回忆录。工具=WhatCable（macOS 顶部菜单栏 USB-C 电缆特征检测）/ amber（简化 Bash 语法新语言、编译成 Bash）/ Ant（轻量 JS/TS 运行时、二进制仅 8MB）/ 屏译（开源安卓实时屏幕翻译）/ TurboOCR（GPU 高速文字识别）+ light-ocr（纯本地 OCR 库 JS/C++ API）/ Visprex（CSV 数据自动可视化）/ Loro（开源 CRDT 多人实时同步算法库）/ File Wizard（自托管文件格式转换+图像 OCR+音频转文字）。AI 相关=Oh My HuggingFace（非官方 HF 跨平台客户端）/ pi-auto-approval（Pi agent 插件、自动批准低风险权限确认、高风险仍交用户——与使用者 auto mode + permission classifier 关注点呼应）/ GPT Crawler（网站内容抓取生成 ChatGPT 聊天机器人）/ Tokenwiz（模仿 OpenAI tokenization 开源实现）。资源=Goto Onion（Tor 暗网网关）/ Fading Maize（AI 将 2001 年乐队录音改编成 2026 风格对比聆听）/ 迷宫算法收藏站。图片=法罗群岛海底环岛（大西洋海底隧道分岔处环岛、水母造型警示）。文摘《AI 本应节省时间，减少工作量》— 加州大学调查发现 AI 反而增加工作量+职业倦怠，原因：(1) 工作职责范围扩大（vibe coding 需工程师审查非程序员代码）(2) 工作/生活界限模糊（休息时间发提示词跑任务）(3) 多任务处理激增 (4) 自我强化循环；解决方法=重要决定前暂停 + 减少上下文切换 + 预留真人际交往时间——与使用者 mission-critical infra 不碰 hobby toolkit 工作风格研究 + AI career landscape 研究直接呼应。言论=霍金「如果你是太阳我就是黑洞」/《奇点越来越近了》「AI 模型世界像城市有五个企业总部区域+一个中国城」/《AI 是一个糟糕的工具》「提示工程完全是骗局、AI 是机器实现的相信我兄弟」/《为什么便利总是胜出》用面包机三年只用两次类比即使 AI 能轻松生成代码 SaaS 仍不会消亡。本期提及 GitHub repo 数条（darrylmorley/whatcable、ciddwd/overlay-translator、arcships/light-ocr、oh-my-hf/ohmyhf、Europa2061/pi-auto-approval、BuilderIO/gpt-crawler、1rgs/tokenwiz 等）——按 digest「GitHub 倉庫觀察」鐵律應集中列出、不散落本段。前一 baseline `403（2026-07-10 发布《为什么 Dropbox 不成功》）`（头条 Dropbox 定位错误论：C端消费者工具而非企业生产力工具是根本失败原因，与使用者 mission-critical-infra 研究呼应；另一亮点 AI 员工人均支出达工资 2.3 倍数据，呼应 AI career landscape 研究；工具栏 Vite+/Davit/Flint/LaTeXSnipper 等）；前一 baseline `402（2026-07-03 发布《我在智念 AI 的日子（小说）》）` — 2026-07-04 probe baseline（头条讽刺 AI 编程职场小说、特斯拉限制员工每周 AI 支出 200 美元、工具栏 Deno Desktop/EdgeMirror/Douzy）；前一 baseline `401（2026-06-20 前后发布《如何赚到10亿美元》）` — 2026-06-26 digest 处理（PG 牛津演讲 / Speedtest 被 12 亿收购 / SQLite 作者 OSS 维护负担 / HTTP QUERY / PACT 协议）；前一 baseline `400（2026-06-12 发布《rsync 的争论》）`；前一 baseline `399（2026-06-05 发布《中国 AI 大厂访问记》）`。

### Entry 範本

```
### <title — 簡短 probe 名>

- **Why**: <為什麼要 probe — context，之後 agent 整理結果時引用>
- **Source(s)**: <去哪查 — URL / repo / API endpoint>
- **How to fetch**: <agent 用什麼工具：`gh release view <owner>/<repo>` / WebFetch / `curl` JSON / Context7 / claude-in-chrome MCP>
- **Hit signal**: <什麼算「有新東西」— version > X / 上次 fetch 時間 / 特定 keyword 出現>
- **Action on hit**: <找到後做什麼 — inject 進當天 digest「外部訊號」段、標 priority、想抓細節 follow up>
- **Triggered by**: <`daily` / `stage-2-digest` / `both`> — `daily` 走 06:45 launchd probes wrapper；`stage-2-digest` 只在 digest pre-flight 由 agent 主動 fetch（適合 release 類低頻 / 高 cost / 需 user attention 的訊號、避免 daily spam）；`both` 兩邊都跑
- **Last seen**（optional）: <上次 baseline 值 — agent 每次 fetch 完更新這欄；空白代表第一次跑>
```

## Resolved / Archived

_(probe 不再 relevant 時移到這、保留歷史、一年後刪)_

## 維護規則

- **Hit signal 必填**：沒寫判準 agent 不知道 baseline 在哪、會每天 report 同樣內容；最簡單寫「上次 fetch 時間 / version / hash」
- **How to fetch 寫具體命令或工具**：不要寫泛泛「查 GitHub」，寫 `gh release view <owner>/<repo> --json tagName,publishedAt`，agent 才能照跑
- **Triggered by 必填**：`daily` / `stage-2-digest` / `both` 三選一。`daily` 走 06:45 launchd 自動跑；`stage-2-digest` 只在使用者叫 stage-2 digest 時 agent 主動 fetch；`both` 兩邊都跑。**daily 跑時 agent 過濾掉 `stage-2-digest` only 的 entries**（讀 PROBES.md 時跳過、不 fetch 不 surface）；**digest 啟動時 agent 讀 PROBES.md 過濾 `stage-2-digest` + `both` entries 跑**（不論 daily 是否已跑過、digest 啟動時都跑一次拿即時 diff）。
- **Last seen 由 agent 更新**：跑完 probe 後若 hit signal 達標，agent 把新 baseline 寫回對應 entry 的 `Last seen` 欄（直接 Edit 本檔，commit 由使用者決定）
- **空 PROBES.md 時 wrapper 跳過**：probes-daily.sh 偵測無 Active entry → 產極簡 report 「今日無 active probe」直接結束

## Output

寫到 `reports/local-analysis/{date}-probes.md`（gitignored，跟其他 4 channel local-analysis 同 dir）。
