# cc-tool 黑名單：已知不追更新的系統 / 開發 / 語言 runtime 工具
# 新發現的工具會列在 daily digest「待分類」，歸這裡 = 永久忽略

# cargo
cargo-bundle

# npm 系統
corepack
npm

# brew（系統 / dev / 語言 runtime / dep）
age
awscli
clang-format
clojure/tools/clojure
cmake
duti
edencommon
ffmpeg
fswatch
fzf
gh
git
git-filter-repo
git-lfs
go
graykode/tap/abtop
ios-deploy
ldid
libgit2
librsvg
llama.cpp
macmon
mactop
mole
mongosh
node@16
pkgconf
poppler
protobuf
python-setuptools
python@3.10
python@3.11
python@3.9
ripgrep
rlwrap
ruby
rust
shopify/shopify/shopify-cli
tree
uv
watchman
yt-dlp
zig
eza
starship
zoxide
zsh-autosuggestions
zsh-completions
zsh-syntax-highlighting

# mcp（stdio 非 npx 的先全忽略；npx 釘版的 MCP 走 manifest 的 mcp-npx manager，2026-08-25 起 chrome-devtools 已移出）
plugin
context7
shopify-dev-mcp
chakra-ui
postman
ako-mkt-admin
ako-cvs-admin
notebooklm-mcp
twinkle-hub
asana

# npm（升版風險 > 收益，使用者拍板永久不追）
# @shopify/cli：2026-08-01 拍板不追。兩個原因疊加——
#   (1) 升了不影響實際跑的東西：PATH 上的 `shopify` 是 brew formula
#       `shopify/shopify/shopify-cli` 3.56.3（/opt/homebrew/bin 在 PATH 第 2 位），
#       npm 這份 4.5.2 裝在 nvm v24.8.0（第 21 位）被完全遮住。
#   (2) 3.x → 4.x 是 major bump，三個工作 repo 各自釘不同版本
#       （akocommerce ^4.4.0 / akohub.com 3.50.0 / retargeting-api ^3.94.3），
#       全域跳版風險高於收益。
# 註：brew 那份早已在下方 brew 段的 ignore 清單裡；本行是把 npm 這份也關掉。
# 同時已從 cc-tool-manifest.json 移除——ignore 只擋「待分類」發現、
# 不擋 manifest 內的更新報告，兩邊都要動才真的不再報。
@shopify/cli

# mcp（同一個 PyPI 套件的第二個註冊，不是獨立工具）
# semble-docs = `uvx --from semble[mcp] semble --content docs`，與 semble server 同套件、
# 只差 --content 旗標 + 獨立快取；版本追蹤由白名單的 semble（uv-tool）覆蓋，
# 列這裡避免同一個套件被報兩次。2026-07-30 使用者拍板。
semble-docs
